---
description: PR 拆分合入主线工作指导 — 开 PR / 改分支前必读
alwaysApply: true
---

# PR 拆分合入（工作指导）

合入 `main`、开/改 PR、从 `lhq-rag-dev` 拣文件前，**先读并遵循**：

[`docs/PR拆分合入主线操作说明.md`](docs/PR拆分合入主线操作说明.md)

处理 **PR #47 / PR-A 评审收尾** 或开大改会话前，再读：

[`docs/PR47_下一步工作摘要.md`](docs/PR47_下一步工作摘要.md)

## 硬性边界（摘要）

| PR | 分支 | 内容 | 顺序 |
|----|------|------|------|
| PR-A | `pr-a-core-engine` | 核心引擎（`raganything/`、pipeline、配图核心、env 模板） | 先合 |
| PR-B | `pr-b-web-ui` | Web、澄清接线、**客户端打包**（`client_*`、`pack_client*`、`web/`） | 依赖 A |
| PR-C | `pr-c-docs` | 文档 | 可与 A 并行 |
| PR-D | `pr-d-dev-tooling` | bench/replay/eval 等工具脚本 | 依赖 B |

## 禁止

- 不要把 PR-B 的打包/Web 脚本塞进 PR-A（#47）
- 不要删除或在「清理误暂存 docs」时丢掉 `docs/PR拆分合入主线操作说明.md`
- 改某个 GitHub PR 时必须在**对应分支**上改并 push，不要只改 `lhq-rag-dev` 却期望 PR 更新
- **禁止** `git merge lhq-rag-dev` → `pr-a-core-engine`（会污染 #47）
- 在 `lhq` 做大改前，须已 `merge pr-a-core-engine` → `lhq-rag-dev`（否则大改不含瘦身）

## 瘦身 ↔ 全量大改（摘要）

1. 瘦身在 `pr-a-core-engine` → push #47（可暂不合 main）  
2. `merge A → lhq-rag-dev` → 全量大改并测  
3. cherry-pick / 拣文件回 `pr-a` → 再合 main  

详见文档 **§十一**。

详细文件清单与 Git 步骤以文档正文为准。
