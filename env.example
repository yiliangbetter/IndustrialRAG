### 配置模板说明（根目录不再维护完整键表）
###
### 绿色安装包 / 客户端 / 本仓库业务开关的唯一完整模板：
###     config/env.example
###
### 开发机跑 pipeline / 脚本（load_dotenv 仓库根 .env）时：
###     PowerShell:  Copy-Item config\env.example .env
###     cmd:         copy config\env.example .env
### 然后编辑 .env 填入 LLM_BINDING_API_KEY 等。
###
### 上游 LightRAG 多存储 / Server 自托管样板（PG、Neo4j 等，绿色包不需要）：
###     reproduce/env.example
### 离线部署说明见 docs/offline_setup.md
###
### 打包脚本优先复制 config/env.example；根目录本文件仅作发现入口，勿再填第二套默认值。
