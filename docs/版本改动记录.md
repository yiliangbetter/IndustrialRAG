# 版本改动记录

约定：**版本号**使用 Git **短提交哈希**（7 位，与 `git log --oneline` 一致）；正式发布若打 `git tag`，可在同一条目下补充标签名。**日期**为该提交的作者日期（本地时区以 `git log` 为准）。

维护方式：**新条目追加在文档最上方**（时间倒序），旧记录向下滚动保留。

每条「代码改动」下必须写 **`改动目的（当时）`**，并附 **代码块**（关键函数/配置/env 片段）；便于以后对照 diff。若与现状不一致，以 `git show <hash>` 为准。

---

## `2ad0483` · 2026-06-10

**提交说明：** `feat(query): Plan A image source, strict object align, maintenance fidelity`

### 本版本目的（当时）

1. **配图 Plan A**：配图只扫 `process_chunks_unified` 送入答案 LLM 的 chunk，避免 rerank 池误配（Q2 page34 电控板）或与答案正文不一致。
2. **对象对齐**：单条保养/检查题从问句抽 concrete object（如 `压带轮残胶`），禁止「检查」+「保护开关」等泛词误配；列举题（「哪些…」）仍走宽松 + `llm_primary_scan`。
3. **anchor 回退**：有 anchor section 但节内无图或图不对齐 → `llm_subject_chunks`；全失败则 0 图。
4. **文字答案**：正文 chunk 无核心对象时，允许用 KG 同名实体作答（Q2 电源开关）；`docs/测试问题.txt` 补充 Q1–Q7 文字/配图期望。

### 概要

| 模块 | 变更 |
|------|------|
| Plan A hook | `query_progress_hooks.py`：`_llm_chunks_for_images`；`finalize_related_images` 只读 LLM chunks |
| 配图扫描 | `image_query_refs.py`：`llm_subject_chunks` / `llm_primary_scan`；`_action_object_cjk`、`_strict_object_image_gate` |
| 答案 steering | `query_doc_steering.py`：`build_maintenance_section_fidelity_prompt` 允许 KG 兜底 |
| 文档 | `问答全流程图.md` §5、`测试问题.txt` Q3 caption、`使用说明.md` §8.4 |

### 代码改动

**1）Plan A 配图数据源（`scripts/query_progress_hooks.py`）**

**改动目的（当时）：** rerank 池常含同页无关条目（电器元件/电控板），与 LLM 实际采用的 chunk 不一致；配图须与答案 LLM 输入严格同源。

```python
def finalize_related_images(*, limit: int = 4) -> list[dict[str, Any]]:
    docs = snap.get("llm_chunks_for_images") or _llm_chunks_for_images.get() or []
    docs_text = text_from_retrieved_docs(docs).strip()
    images, debug = resolve_query_images(docs_text, roots, query=q, retrieved_docs=docs, ...)
    debug["image_source"] = "llm_chunks"
```

**2）对象对齐与 anchor 回退（`scripts/image_query_refs.py`）**

**改动目的（当时）：** Q2 因「开关∈保护开关」「检查∈检查电控板」误配；Q5/Q6 因问句尾部「什么工具/加注什么」被当成 object 导致 0 图。

```python
def _action_object_cjk(query: str) -> str:
    # 去掉 什么工具 / 加注什么 / 多长时间 等问句框架，再 strip 清理/检查 前缀
    ...

def _context_for_image_scan(...):
    if not anchor_sections:
        subject_scan = _llm_chunks_with_subject_figures(q, docs)
        if subject_scan.strip():
            meta["fallback"] = "llm_subject_chunks"
            return subject_scan, meta
    # anchor 节内无图或图不对齐 → 同样 llm_subject_chunks
```

**3）维护条目 KG 兜底（`scripts/query_doc_steering.py`）**

**改动目的（当时）：** Q2 正文 chunk 无「电源开关」条目时，LLM 有时严格拒答；KG 实体已含标准表述时应可作答。

```python
"若正文 chunk 均未出现问句核心操作对象…而知识图谱中存在与该对象同名的实体或关系且直接回答该问句，"
"可以该图谱描述作答；仍不得混入其它条目的周期、油品或其它检查项。"
```

---

## `1ae3f77` · 2026-06-09

**提交说明：** `feat(ingest+query): table-aware ingest, row-matrix boost, image anchor fixes`

### 本版本目的（当时）

1. **表格检索**：维护手册润滑/保养表在向量检索中易丢失整表或单行；需在灌库保留完整 `[Table]` HTML，并建 **行矩阵** 供查询 term 对齐补 chunk（替代已废弃的 `[TableFlat]` flatten）。
2. **列举题配图**：「哪些部件要清理残胶」等跨多节问题在 `RAG_IMAGE_ANCHOR_MODE=answer` 下 `no_anchor` 导致 0 图；需在 primary 含 inline 图时 **回退整段 primary** 扫描。
3. **灌库运维**：Web/CLI 同名 PDF 重传应替换旧索引，避免重复 doc 与 matrix 残留。

### 概要

| 模块 | 变更 |
|------|------|
| 表感知灌库 | `utils.py`：`[Table]` 边界分段、按 `<tr>` 切超大表并保留表头；`insert_text_content` 用 segment delimiter |
| 行矩阵 | 新建 `raganything/table_matrix.py` → `kv_store_table_matrix.json`；灌库 `persist_table_matrix_after_ingest` |
| 查询补 table | `query_doc_steering.supplement_table_matrix_chunks`（rerank 后注入，`rerank_score≈0.98`） |
| 表 listing prompt | `detect_table_filter_signal` + context 确认后注入 listing 约束（如 Q10 润滑脂） |
| 配图 | `image_query_refs`：废弃 `focus_text_for_images`；`_context_for_image_scan` + `primary_no_anchor` 回退 |
| 重灌替换 | `rag_pipeline_parse_graph_chat._remove_existing_docs_for_file` + `delete_matrix_for_doc` |
| 清理 legacy | 删除 `[TableFlat]` / embed hook；`strip_table_flat_from_chunks.py` |
| 批测 | `compare_query_tuning.py`、`query_tuning_profiles.py` |
| 文档 | `问答全流程图.md`、`使用说明.md`、`文档解析灌库与图谱问答脚本说明.md` |
| Web 文案 | `app.js` / `setup.js` 上传灌库说明（单文件、同名替换） |

### 代码改动

**1）表感知分段（`raganything/utils.py`）**

**改动目的（当时）：** MinerU 表格 HTML 若与普通段落混切，检索时表头/行被拆散；每个 `[Table]` 块应独立成 chunk，超长表按行切且每段带表头。

```python
def prepare_table_aware_ingest_segments(document_parts, tokenizer, max_tokens):
    merged = build_table_aware_ingest_segments(document_parts)
    for seg in merged:
        if _TABLE_INGEST_MARKER in seg:
            prepared.extend(_split_table_html_segment(seg, tokenizer, max_tokens))
    ...

async def insert_text_content(..., document_parts=None):
    if table_aware_ingest_enabled() and document_parts:
        segments = prepare_table_aware_ingest_segments(...)
        blob = _INGEST_SEGMENT_DELIMITER.join(segments)
        await lightrag.ainsert(input=blob, split_by_character=_INGEST_SEGMENT_DELIMITER,
                               split_by_character_only=True, ...)
```

**2）行矩阵灌库（`raganything/table_matrix.py` + `processor.py`）**

**改动目的（当时）：** 向量检索对「季度保养 + 美孚」等跨列表述不稳定；将每表解析为 `{headers, rows, caption, chunk_ids}`，查询时用 `text_term_alignment_symmetric` 对 **行文本** 打分，通用、非 needle 词表。

```python
@dataclass
class TableMatrixRecord:
    table_id: str
    headers: list[str]
    rows: list[list[str]]
    chunk_ids: list[str]
    caption: str = ""

async def persist_table_matrix_after_ingest(lightrag, *, full_doc_id, ingest_segments, ...):
    records = build_records_from_segments(ingest_segments, ...)
    link_records_to_chunks(records, all_chunks, full_doc_id=full_doc_id)
    upsert_matrix_for_doc(working_dir, records, full_doc_id=full_doc_id)
```

**3）查询补 table chunk（`query_doc_steering.py`）**

**改动目的（当时）：** rerank top-k 未含目标 `[Table]` chunk 时，从 matrix 按 query 对齐注入已灌库 chunk 正文，使 LLM context 含完整表行。

```python
def supplement_table_matrix_chunks(query, docs, *, rerank_pool=None):
    for record in _load_table_matrix_from_storage():
        if not table_matrix_matches_query(query, record, ...):
            continue
        for chunk_id in record.chunk_ids:
            boosted.append({"id": chunk_id, "rerank_score": 0.98, ...})
    return boosted + docs
```

**4）列举题配图回退（`scripts/image_query_refs.py`）**

**改动目的（当时）：** Q11「哪些部件要清理残胶」跨 2.1.4/2.1.5/3.3.x，`answer` anchor 选不出单节 → `figure_context` 为空 → gate 失败 0 图；primary 内已有 inline 图时应回退整段扫描。

```python
if not anchor_sections:
    meta["reason"] = "no_anchor"
    if extract_image_refs_from_context(primary):
        meta["fallback"] = "primary_no_anchor"
        return primary, meta
    return "", meta
```

**5）同名 PDF 替换（`rag_pipeline_parse_graph_chat.py`）**

**改动目的（当时）：** Web 单文件重灌或 CLI 重复灌同一文件名时不应叠加 duplicate doc_id。

```python
async def _remove_existing_docs_for_file(rag, rel: str) -> int:
    # match doc_status by basename → adelete_by_doc_id + delete_matrix_for_doc
```

env 节选：

```bash
RAG_TABLE_AWARE_INGEST=true
RAG_TABLE_MATRIX_INGEST=true
RAG_TABLE_MATRIX_QUERY_BOOST=true
RAG_TABLE_MATRIX_MIN_ALIGN=0.08
RAG_TABLE_MATRIX_MIN_ROWS=2
# RAG_IMAGE_ANCHOR_MODE=answer   # Web/compare 默认；列举题依赖 primary_no_anchor 回退
```

### 验证（当时）

- Q10（润滑脂表）：`compare_query_tuning.py --ids 10` — matrix 补检索后 9 行美孚进 context
- Q11（残胶列举）：`--ids 11` — gate ok，≥2 张 inline 图（压带轮、仿形靠板）；第三张依赖 primary 是否含涂胶轴 chunk
- 重灌后 `kv_store_table_matrix.json` 与 `[Table]` chunk 的 `chunk_ids` 一致

---

## `ae7ca9a` · 2026-06-06

**提交说明：** `fix(query): restore retrieval_scope SSE and align Q&A flow docs`

### 概要

- 修复 `process_chunks` hook 内 `consume_filter_report` → **`retrieval_scope` SSE** 未推送的问题（`query_progress_hooks.py`）
- 同步更新 `docs/问答全流程图.md`、`docs/多轮问答实现方案.md` 与现网澄清/配图时序

---

## `85ef6b9` · 2026-06-06

**提交说明：** `feat(query): add multi-turn clarification gate for vague queries`

### 本版本目的（当时）

模糊/低匹配问句（如「怎么保养」「加什么油」）若直接进入 `aquery`，易跨手册乱答或空答。需在 **正式检索与答案 LLM 之前** 增加可关闭的澄清门控：轻量评估 → LLM 生成候选问法 → 再检索验证 → Web 展示选项；用户选定或「仍用原问题」后再走现网 `aquery` + 配图流程。

**规模（提交当时）：** 11 个文件，+1659 / −11 行。

### 概要

| 模块 | 变更 |
|------|------|
| 澄清门控 | 新建 `scripts/query_clarification.py`：`assess_query_fit`、`run_clarification_gate`、session |
| Web SSE | `rag_web_server._query_stream_events` 插入门控；`clarification` / `clarification_done` / `gate_skip` |
| 请求体 | `QueryBody.session_id`、`clarification_choice`（含 `use_original`） |
| 前端 | `app.js` 澄清面板；**选项在作答后保留**，可重复点选其它推荐问法 |
| 调试 | `scripts/probe_query_clarify.py`；`POST /api/query/assess` |
| 配置 | `env.example`：`QUERY_CLARIFY_*`、`QUERY_SCORE_THRESHOLD_*` |
| 文档 | `docs/多轮问答.txt`、`docs/多轮问答实现方案.md`、`docs/web路径` |

### 代码改动

**1）门控评估与 band（`query_clarification.py`）**

**改动目的（当时）：** 澄清阶段不调答案 LLM；用与 `dump_query_context` 同路径的 `aquery_data` + rerank，**top-3 平均** 为 score，划分 `clear` / `vague` / `unrelated`。门控默认 **naive + 较小 top_k**，避免完整 `mix` 门控过慢。

```python
def band_from_score(score: float, cfg: ClarifyConfig) -> Band:
    if score >= cfg.threshold_upper:
        return "clear"
    if score < cfg.threshold_lower:
        return "unrelated"
    return "vague"

def assess_query_param_from_env() -> Any:
    assess_mode = (os.getenv("QUERY_CLARIFY_ASSESS_MODE") or "naive").strip()
    param = query_param_from_env(mode=assess_mode)
    param.top_k = _int("QUERY_CLARIFY_TOP_K", 12)
    param.chunk_top_k = _int("QUERY_CLARIFY_CHUNK_TOP_K", 12)
    return param
```

**2）Case A/B 候选生成 + 再检索打分**

**改动目的（当时）：** 对齐 `docs/多轮问答.txt`——**A（unrelated）** 仅原问送 LLM 生成 k 条；**B（vague）** 原问 + `preview_snippets`；每条候选并行 `assess_query_fit`，策略 `all` 返回过线项。

```python
async def generate_alternative_queries(rag, query, *, band, preview_snippets, k):
    if band == "unrelated":
        user = f"用户原问题：{query}"
    else:
        ctx = "\n".join(f"- {s}" for s in preview_snippets[:6])
        user = f"用户原问题：{query}\n\n检索摘要：\n{ctx}"
    return await _llm_rewrite_queries(rag, user_prompt=user, system_prompt=system)
```

**3）高分仍模糊：LLM 具体性判断（非词表）**

**改动目的（当时）：** 「怎么保养」等广检索可得高分 `clear`，但语义仍模糊。检索分达 upper 时可选 `QUERY_CLARIFY_LLM_SPECIFICITY_CHECK`，由 LLM 读原问 + 摘要输出 `specific_enough`，为 false 则降为 `vague` 进入澄清（**不用**设备名/口语片段硬编码）。

```python
if effective_band == "clear" and cfg.llm_specificity_check:
    specific = await llm_is_query_specific_enough(rag, q, assess.preview_snippets)
    if specific is False:
        effective_band = "vague"
```

**4）Web SSE 门控与续轮（`rag_web_server.py`）**

**改动目的（当时）：** 首轮在 `aquery` 前跑 `run_clarification_gate`，门控状态经队列边跑边 `yield`；`action=clarify` 时发 `clarification` + `clarification_done` 并 **return**，不进入答案流。续轮若带 `clarification_choice` 则 `resolve_query_for_rag` 后直接 `aquery`。

```python
# scripts/rag_web_server.py
if (clarification_choice or "").strip():
    q, gate_reason = resolve_query_for_rag(original_q, clarification_choice, session_id)
elif clarify_enabled():
    outcome = await run_clarification_gate(state.rag, original_q, on_status=_on_clarify_status)
    if outcome.action == "clarify":
        yield _sse(clarification_to_sse_payload(outcome))
        yield _sse({"type": "clarification_done"})
        return
```

```python
class QueryBody(BaseModel):
    session_id: str | None = Field(None, description="Clarification session from prior clarification SSE.")
    clarification_choice: str | None = Field(
        None, description='Selected rewritten query, or "use_original" to skip gate.'
    )
```

**5）前端选项持久（`app.js`）**

**改动目的（当时）：** 点选推荐问法后 **不 replaceWith** 澄清面板，在下方 `createLoadingMessage()` 追加作答流；`session_id` 写在面板 `dataset` 上供重复点选。

```javascript
async function resumeClarifiedQuery({ originalQuery, choice, sessionId, clarifyPanelEl, clickedBtn }) {
  const loading = createLoadingMessage();
  await streamQuery(
    { query: q, clarification_choice: choice, session_id: sid },
    loading
  );
}
```

env 节选：

```bash
QUERY_CLARIFY_ENABLED=true
QUERY_SCORE_THRESHOLD_LOWER=0.28
QUERY_SCORE_THRESHOLD_UPPER=0.45
QUERY_CLARIFY_LLM_SPECIFICITY_CHECK=true
```

**6）调试入口（`probe_query_clarify.py` + `/api/query/assess`）**

**改动目的（当时）：** 命令行与 HTTP 可单独看 band/score，不跑答案 LLM、不改 Web UI。

```bash
.\.venv\Scripts\python.exe scripts\probe_query_clarify.py "日常维护要注意什么"
.\.venv\Scripts\python.exe scripts\probe_query_clarify.py "加什么油" --assess-only
```

```python
@app.post("/api/query/assess")
async def api_query_assess(body: QueryBody):
    assess = await assess_query_fit(state.rag, q, mode=mode)
    return {"band": assess.band, "score": assess.score, "preview_snippets": assess.preview_snippets, ...}
```

---

## `0a7178e` · 2026-06-04

**提交说明：** `fix(query): align Web related images with reranked chunks used by the LLM`

### 本版本目的（当时）

Web 流式问答配图与批测/`aquery` 实际喂给 LLM 的 chunk 不一致：LightRAG `operate.py` 在模块加载时绑定 `process_chunks_unified`，仅 patch `lightrag.utils` 无效；mix 路径常跳过 rerank hook 写 ContextVar，导致 `retrieval.docs` 为空、Web 无图或错图（如注油泵案例）。

**规模（提交当时）：** 6 个文件，+1144 / −325 行。

### 概要

| 模块 | 变更 |
|------|------|
| Hook | 同时 patch `operate` + `utils` 的 `process_chunks_unified`；返回后 `_sync_chunks_for_images` |
| 配图收尾 | `finalize_related_images` 优先读 `_query_debug_snapshot`，空 rerank 不 wipe 已有 snapshot |
| 测试 | `raganything/pipeline_rerank.py` 暴露 `query_param_from_env` 等；批测走 Web 同路径 |
| 配图算法 | `image_query_refs.py` focus 门控收紧；`query_doc_steering` 小修 |
| 用例 | `docs/测试问题` 配图期望微调 |

### 代码改动

**1）双绑定 `process_chunks_unified`（`query_progress_hooks.py`）**

**改动目的（当时）：** `kg_query` 内部调用的是 `operate.process_chunks_unified`（import 时副本），必须在 **operate 与 utils 两处** 打补丁，并在 unified 处理 **返回后** 同步 chunk 列表（rerank + steering 过滤后），与 LLM 上下文一致。

```python
orig_process_chunks = op.process_chunks_unified

async def _process_chunks_unified(*args, **kwargs):
    chunks = await orig_process_chunks(*args, **kwargs)
    query = kwargs.get("query") or (args[0] if args else "")
    if isinstance(query, str) and chunks:
        _sync_chunks_for_images(query, chunks)
    return chunks

op.process_chunks_unified = _process_chunks_unified
ut.process_chunks_unified = _process_chunks_unified
```

**2）finalize 从 snapshot 取 docs（`query_progress_hooks.py`）**

**改动目的（当时）：** rerank 常在 LightRAG worker task 内执行，父协程 ContextVar 可能为空；用 `_query_debug_snapshot` 合并 worker 写入的 `retrieved_docs`。mix 偶发空 rerank 列表时 **不清空** 已有 `process_chunks` snapshot。

```python
def finalize_related_images(*, limit: int = 4) -> list[dict]:
    snap = get_query_debug_state()
    docs = list(snap.get("retrieved_docs") or _retrieved_docs.get() or [])
    docs_text = snap.get("retrieved_docs_text") or text_from_retrieved_docs(docs)
    if not docs_text:
        ctx = snap.get("retrieval_context") or _retrieval_context.get()
        docs_text = merge_context_for_images("", ctx)
    images, debug = resolve_query_images(docs_text, roots, query=q, retrieved_docs=docs, limit=limit)
    return images
```

**3）Web 收尾配图与 rerank 参数复用（`rag_web_server.py` + `pipeline_rerank.py`）**

**改动目的（当时）：** 流式结束在发 `done` 前调用 `finalize_related_images()`，与 hook 写入的 **process_chunks 后** snapshot 同源；抽取 `query_param_from_env` 供脚本/门控复用，避免各处手写 `QueryParam`。

```python
# scripts/rag_web_server.py — token 流结束后
from query_progress_hooks import finalize_related_images

related = finalize_related_images()
if related:
    yield _sse({"type": "related_images", "images": related})
```

```python
# raganything/pipeline_rerank.py
def query_param_from_env(*, mode: str | None = None) -> Any:
    from lightrag import QueryParam
    m = (mode or os.getenv("RAG_QUERY_MODE") or "mix").strip()
    kwargs: dict[str, Any] = {"mode": m, "enable_rerank": _env_bool("RERANK_BY_DEFAULT", True)}
    ...
    return QueryParam(**kwargs)
```

**4）空 rerank 不清 snapshot（`query_progress_hooks.py`）**

**改动目的（当时）：** mix 偶发对空列表调用 rerank 同步；若直接 `set([])` 会冲掉 `process_chunks_unified` 已写入的好 chunk。

```python
def _sync_retrieved_docs_after_rerank(final_docs: list[dict]) -> None:
    if not final_docs:
        # Mix may call rerank with empty lists; do not wipe a good process_chunks snapshot.
        return
    _retrieved_docs.set(final_docs)
    ...
```

---

## `02dc2b4` · 2026-06-03

**提交说明：** `feat(query): boost catalog product-model listing across all edge-band manuals`

### 本版本目的（当时）

「南兴封边机一共有多少产品型号？」等 **跨手册目录/型号列举** 题，rerank 后常缺各手册前言「本手册适用产品型号」chunk，答案只列部分机型。需在 rerank 后 **按手册路径补全** 目录 chunk，且目录题 temporarily 放宽 `min_rerank_score`。

**规模（提交当时）：** 2 个文件，+202 行。

### 代码改动

**1）目录 chunk 补全（`query_doc_steering.py`）**

**改动目的（当时）：** 识别 broad catalog 问句（非单机保养 profile），从当前 docs、rerank pool、KV 存储加载带 `本手册适用产品型号` 标记的 chunk，按 `file_path` 去重并抬高 `rerank_score`，再拼回列表前部。

```python
def is_catalog_product_model_query(query: str) -> bool:
    if resolve_machine_profile(query):
        return False
    asks_scope = bool(
        re.search(
            r"哪些|多少|一共|总共|全部|有哪些|几种|列举|清单|概况|多少个|一共有多少|多少种",
            q,
        )
    )
    asks_models = bool(re.search(r"型号|机型|产品", q))
    return asks_scope and asks_models

def supplement_catalog_product_model_chunks(query, docs, *, rerank_pool=None) -> list[dict]:
    if not _env_bool("RAG_CATALOG_QUERY_BOOST", True):
        return docs
    if not is_catalog_product_model_query(query):
        return docs
    ...
    boosted["rerank_score"] = max(float(boosted.get("rerank_score") or 0), 0.99)
    return merged + [d for d in docs if _doc_path(d) not in seen_paths]
```

**2）目录题 rerank 阈值（`install_catalog_rerank_threshold`）**

**改动目的（当时）：** 目录 chunk 与问句字面相似度低，易被 `min_rerank_score` 滤掉；目录题期间将 `global_config["min_rerank_score"]` 降为 `RAG_CATALOG_QUERY_MIN_RERANK_SCORE`（默认 0），事后恢复。

```python
def install_catalog_rerank_threshold() -> None:
    async def _wrapped(query, unique_chunks, query_param, global_config, ...):
        prev_min = None
        if is_catalog_product_model_query(query) and _env_bool("RAG_CATALOG_QUERY_RERANK", True):
            prev_min = float(global_config.get("min_rerank_score", _default_min_rerank_score()))
            global_config["min_rerank_score"] = catalog_query_min_rerank_score()
        try:
            return await orig(query, unique_chunks, query_param, global_config, ...)
        finally:
            if prev_min is not None:
                global_config["min_rerank_score"] = prev_min
    ut.process_chunks_unified = _wrapped
```

**3）rerank 后挂载补全（`install_doc_filter_on_rerank`）**

**改动目的（当时）：** 在 `apply_rerank_if_enabled` 包装层里调用 `supplement_catalog_product_model_chunks`，再跑 `filter_retrieved_docs_with_report`，与现网 chunk 过滤同一出口。

```python
docs = await orig(query, retrieved_docs, global_config, enable_rerank, top_n)
docs = supplement_catalog_product_model_chunks(query, docs, rerank_pool=docs)
kept, _report = filter_retrieved_docs_with_report(query, docs)
return kept
```

env 节选：

```bash
RAG_CATALOG_QUERY_BOOST=true
RAG_CATALOG_QUERY_RERANK=true
RAG_CATALOG_QUERY_MIN_RERANK_SCORE=0
```

---

## `b370cad` · 2026-06-03

**提交说明：** `feat(query): KG file_path filter, multi-figure listing, ingest UX`

### 本版本目的（当时）

1. **多手册 mix 检索**：图谱实体/关系的 `file_path` 为 `<SEP>` 拼接，steering 的 deny 只滤 chunk、未滤 KG 上下文，导致错机型实体混入。
2. **列举类配图**：「哪些部件要清理残胶」需 **多图**（仿形靠板/压带轮/涂胶轴各一张），单图逻辑不够。
3. **灌库/安装向导**：Web 灌库进度、安装向导与客户端 setup 体验补强；答案去掉 LLM 复制的 ①②③ 圈号。

**规模（提交当时）：** 12 个文件，+1542 / −204 行。

### 概要

| 模块 | 变更 |
|------|------|
| KG 过滤 | `install_kg_file_path_filter`：truncation 前按 profile `deny_path_substrings` 滤实体/关系 |
| 多图列举 | `image_query_refs.py`：`_listing_targets_*`、多主题各选一张、`RAG_MULTI_FIGURE_LIMIT` |
| 答案清洗 | `strip_manual_circled_step_markers`：流式/最终答案去圈号 |
| 灌库 Web | `ingest_ui.js`、`rag_web_server` ingest API 与进度展示 |
| 安装向导 | `setup.html` / `setup.js` / `client_setup_service.py` 调整 |

### 代码改动

**1）KG `file_path` 按手册过滤（`query_doc_steering.py`）**

**改动目的（当时）：** mix 模式在 token truncation 前对 `search_result` 的 entities/relations 按匹配 profile 的 deny 规则过滤 `<SEP>` 合并路径，与 chunk 侧 `filter_retrieved_docs_by_query` 一致。

```python
def filter_kg_search_by_query(query: str, search_result: dict) -> tuple[dict, dict | None]:
    deny, profile = active_deny_substrings(query)
    if not deny:
        return search_result, None
    for ent in search_result.get("final_entities") or []:
        kept = _filter_sep_merged_kg_record(ent, deny)  # 按 <SEP> 拆分 file_path
        ...

def install_kg_file_path_filter() -> None:
    async def _apply_token_truncation(search_result, query_param, global_config):
        q = (_kg_filter_query.get() or "").strip()
        if q:
            filtered, _ = filter_kg_search_by_query(q, search_result)
            search_result = filtered
        return await orig_trunc(search_result, query_param, global_config)
```

**2）多主题列举配图（`image_query_refs.py`）**

**改动目的（当时）：** 从检索正文 focus 行解析多个保养主题（锚定在正文，非问句词表），每个主题最多选一张通过对齐门的图；`query_wants_multi_figure_listing` 判定列举场景。

```python
def _listing_targets_with_query_line_overlap(query, full_text, anchor_text=None) -> list[str]:
    targets = _listing_targets_anchored_in_text(query, full_text, anchor_text=focus)
    # 保留与 query 行有重叠且在正文中的主题短语
    ...

def _ref_aligns_for_multi_figure_listing(query, ref, retrieved_text, ...) -> bool:
    for target in _listing_targets_with_query_line_overlap(query, source, anchor):
        if _label_matches_listing_target(label, target):
            return True

def _multi_figure_image_limit() -> int:
    raw = os.getenv("RAG_IMAGE_MULTI_LIMIT") or "4"
    return max(2, min(6, int(raw)))
```

**3）答案圈号清洗（`query_doc_steering.py` + `rag_web_server.py`）**

**改动目的（当时）：** 手册原文无 ①②③，LLM 却常带圈号列表；在 `answer_delta` 与最终 `answer` 上剥离圈号前缀，保留中文正文。

```python
_CIRCLED_STEP_BEFORE_CJK = re.compile(
    r"[①②③④⑤⑥⑦⑧⑨⑩…](?=\s*[\u4e00-\u9fff])"
)

def strip_manual_circled_step_markers(text: str) -> str:
    return _CIRCLED_STEP_BEFORE_CJK.sub("", text)
```

```python
# scripts/rag_web_server.py — 流式 delta 与最终答案均清洗
piece = strip_manual_circled_step_markers(piece)
yield _sse({"type": "answer_delta", "text": piece})
final_answer = strip_manual_circled_step_markers("".join(answer_parts).strip())
```

**4）Web 灌库 SSE（`rag_web_server.py` + `ingest_ui.js`）**

**改动目的（当时）：** 浏览器上传 PDF 后走流式灌库，展示保存/进度/完成/取消；灌库期间 `ingest_active` 锁住问答与配置保存，避免并发写库。

```python
# scripts/rag_web_server.py
async def _ingest_stream_events(files: list[UploadFile]) -> AsyncIterator[str]:
    state.ingest_active = True
    ok, fail, errors, cancelled = await _run_ingest_on_folder(tmp_root, on_event=on_event)
    await queue.put({"type": "done", "ok": ok, "fail": fail, "errors": errors})
```

```javascript
// web/static/ingest_ui.js
const res = await fetch("/api/ingest/stream", { method: "POST", body: fd });
// 解析 SSE：ingest_saved / progress / done / cancelled
```

**5）安装向导（`setup.html` + `client_setup_service.py`）**

**改动目的（当时）：** 客户端首次启动可走 Web `/setup` 填写 LLM/Embedding 等 env，保存后触发 RAG 重载；与主问答页分离，减少手工改 `.env`。

```python
# scripts/client_setup_service.py — 读写用户 data 目录下 .env 片段
@app.get("/api/setup/status")
@app.post("/api/setup/env")
```

env 节选（KG 过滤）：

```bash
RAG_QUERY_KG_FILE_FILTER=true
RAG_IMAGE_MULTI_LIMIT=4
```

---

## `49e3707` · 2026-06-02

**提交说明：** `refactor: externalize steering rules and tighten query-image matching`

### 本版本目的（当时）

现场联调配图与多手册检索时暴露三类问题：

1. **配图误匹配**：如「开机前检查电源开关」（§1.1 纯文字、无图）却展示第 34 页「检查电器元件」；「机床内部清洁」等长问句又曾配不到图。
2. **领域规则硬编码**：机型列表、寒暄正则、传动丝杆专项打分、`scrub_kg_context` 等写在 Python 里，换知识库/产品线需改代码。
3. **排障困难**：Web UI 与 hook  worker 的 ContextVar 状态不一致，query dump 里 `images.debug` 为空；重复灌同名 PDF 导致索引条数虚高。

本批改动目标：**steering 外置 JSON**、**配图用长度/子句/bigram 通用算法（不写领域词表）**、**结构化 query dump + 开发面板**、**灌库按文件名替换旧索引**。

**规模（提交当时）：** 22 个文件，+2240 / −532 行。

### 概要

| 模块 | 变更 |
|------|------|
| 机型 steering | `MACHINE_PROFILES` 迁出 → `config/query_steering_profiles.json`；`RAG_QUERY_STEERING_PROFILES` |
| 配图匹配 | `image_query_refs.py` 重写门槛：子句 substance 打分、object bigram 门控、封面/目录过滤、仅 rerank 正文配图 |
| 通用算法 | `raganything/utils.py`：`substantive_bigrams`、`short_label_bag_aligns`、`resolve_image_caption/footnote` |
| 删除 | `scrub_kg_context` / `install_kg_context_scrub`（改由 profile 可选 `steering_prompt`） |
| 灌库 | `_remove_existing_docs_for_file()`：同名 PDF 再灌时先删旧 doc_id |
| 调试 | `scripts/query_debug_dump.py`；Web「开发」面板开关 `RAG_QUERY_DEBUG_DUMP` |
| Hook | `query_progress_hooks.py`：rerank 后 emit 配图；`_query_debug_snapshot` 供 dump 读取 |
| Web | `/api/dev/query-debug`、`/api/media/image`；health 返回 dump 目录 |
| 打包/文档 | `pack_client.ps1` 复制 steering JSON；机型说明改指向 JSON |

### 代码改动

**1）机型 steering 外置 JSON（`query_doc_steering.py` + `config/query_steering_profiles.json`）**

**改动目的（当时）：** 四机型 deny/query 规则原先硬编码在 `MACHINE_PROFILES`，客户追加机型或改 deny 子串必须改 Python。改为 JSON 文件 + env 覆盖，打包时随 `config/` 下发。

```python
# scripts/query_doc_steering.py
_DEFAULT_PROFILES_PATH = _ROOT / "config" / "query_steering_profiles.json"

@lru_cache(maxsize=1)
def _load_profiles_from_file() -> list[dict[str, Any]]:
    path = _profiles_path()
    if not path.is_file():
        return []
    data = json.loads(path.read_text(encoding="utf-8"))
    return data if isinstance(data, list) else []

def load_steering_profiles() -> list[dict[str, Any]]:
    return _load_profiles_from_file() + _load_extra_profiles()
```

```json
// config/query_steering_profiles.json（节选）
{
  "id": "high_speed_smart",
  "label": "高速智能封边机",
  "query_phrases": ["高速智能封边机", "高速智能", "NB9-Smart", "NB10-Smart"],
  "deny_path_substrings": [
    "自动封边机维护保养手册",
    "高速自动封边机维护保养手册"
  ],
  "steering_prompt": "用户询问输送链条等部件保养时，勿引用其它机型手册中的导轨油或集中润滑周期；以本机型手册正文 chunk 为准。"
}
```

env 新增：

```bash
RAG_QUERY_STEERING_PROFILES=config/query_steering_profiles.json
# 仍可追加：RAG_QUERY_DOC_FILTER_RULES_JSON=[{...}]
```

**2）删除 KG 硬编码清洗，改为 profile 可选 `steering_prompt`**

**改动目的（当时）：** `scrub_kg_context` 针对「输送链条 + 长城导轨油」等写死替换规则，不可复用到其它产品线。删除 `install_kg_context_scrub`；需要时在 JSON profile 写 `steering_prompt`，由 `build_steering_user_prompt()` 注入 LLM。

```python
# scripts/query_doc_steering.py（删除 scrub 后的注入方式）
def build_steering_user_prompt(query: str) -> str:
    profile = resolve_machine_profile(query)
    if not profile:
        return ""
    extra = str(profile.get("steering_prompt") or "").strip()
    parts = [
        f"用户问题针对「{profile.get('label')}」。只引用与该来源对应手册的正文 chunk；"
        "若知识图谱实体描述与其它文档合并后冲突，以 chunk 正文为准。"
    ]
    if extra:
        parts.append(extra)
    return " ".join(parts)
```

**3）配图：子句 substance + object bigram 门控（`image_query_refs.py`）**

**改动目的（当时）：**

- 长问句「我将要对…进行开机，开机前我要如何检查电源开关？」与图注「检查电器元件」仅因共享「检查/查电」bigram 被误配（答案在 §1.1 无图）。
- 「机床内部进行清洁，请问步骤是什么？」需配第 5 页「清洁机床内部」，但不能写「请问/步骤」等领域停用词表。

做法：按标点切子句 → 用 `discriminative_terms` 给子句打分 → 取主题子句末尾 4 字 CJK 的 bigram 作为 **object focus**；图注必须与之相交，且需与检索答案行对齐（移除 bag 对齐捷径）。

```python
# scripts/image_query_refs.py
def _query_clauses(query: str) -> list[str]:
    """Split the query on punctuation only (no phrase stripping)."""
    clauses: list[str] = []
    for part in re.split(r"[，,？?！!；;]", query or ""):
        part = part.strip()
        cjk = "".join(re.findall(r"[\u4e00-\u9fff]", part))
        if len(cjk) >= _min_substantive_term_len():
            clauses.append(part)
    return clauses

def _subject_action_clauses(query: str) -> list[str]:
    clauses = _query_clauses(query)
    if len(clauses) <= 1:
        return clauses
    scored = [(_clause_substance_score(clause, query), clause) for clause in clauses]
    best = max(score for score, _ in scored)
    threshold = max(4, int(best * 0.45))
    picked = [clause for score, clause in scored if score >= threshold]
    return picked or [max(scored, key=lambda pair: pair[0])[1]]

def _subject_object_bigrams(query: str) -> set[str]:
    focus: set[str] = set()
    for clause in _subject_action_clauses(query):
        cjk = "".join(re.findall(r"[\u4e00-\u9fff]", clause))
        if len(cjk) >= 4:
            focus |= substantive_bigrams(cjk[-4:])  # 如「电源开关」→ 电源/源开/开关
    return focus

def _ref_passes_focus_bigram_gate(query, ref, retrieved_text=None) -> bool:
    label_bgs = substantive_bigrams(_ref_effective_label(ref))
    object_focus = _subject_object_bigrams(query)
    if object_focus and not (object_focus & label_bgs):
        return False  # 「检查电器元件」无 电源/开关 → 拒绝
    ...
```

**验证用例（提交当时 e2e）：**

| 问题 | 期望配图 |
|------|----------|
| 开机前如何检查电源开关 | 0 张 |
| 机床内部清洁步骤 | 1 张（page≈5，caption=清洁机床内部） |

**4）短图注 bag 对齐（`raganything/utils.py`）**

**改动目的（当时）：** 图注「清洁机床内部」与问句「机床床身清洁」语序不同，纯子串匹配失败。用 CJK bigram 集合交集 + 最短 focus 子串，**无领域词表**。

```python
# raganything/utils.py
def substantive_bigrams(text: str) -> set[str]:
    bigrams: set[str] = set()
    for run in re.findall(r"[\u4e00-\u9fff]+", text or ""):
        for i in range(len(run) - 1):
            bigrams.add(run[i : i + 2])
    return bigrams

def short_label_bag_aligns(query: str, label: str, *, min_shared: int = 2) -> bool:
    shared = substantive_bigrams(query) & substantive_bigrams(label)
    if len(shared) < min_shared:
        return False
    focus = _focus_run_for_bag(query, label)
    ...
```

**5）配图仅在 rerank 正文路径 emit（`query_progress_hooks.py`）**

**改动目的（当时）：** 若在 `process_chunks` hook 配图，LightRAG 模块加载时已绑定旧函数，monkeypatch 不生效；且全局 context 会 bleed 无关 `[图片]` 块。改为 **rerank 完成后** 只对 `text_from_retrieved_docs(docs)` 解析配图，并通过 `_query_debug_snapshot` 让父协程 dump 能读到 worker 内状态。

```python
# scripts/query_progress_hooks.py
async def _emit_related_images(retrieved_docs: list[dict] | None = None) -> None:
    docs = _latest_retrieved_docs(retrieved_docs) or []
    docs_text = text_from_retrieved_docs(docs).strip()
    _last_image_debug.set(
        explain_query_images(docs_text, roots, query=_query_text.get(), retrieved_docs=docs)
    )
    images = images_for_api(docs_text, roots, query=_query_text.get(), retrieved_docs=docs)
    if images:
        q.put_nowait({"type": "related_images", "images": images})
    _sync_query_debug_snapshot()
```

**6）结构化 query debug dump（`query_debug_dump.py` + `rag_web_server.py`）**

**改动目的（当时）：** 配图/检索/regression 需落盘完整上下文；开发面板一键开关，dump 写入 `logs/query_dumps/*.json`（schema `rag_query_dump_v1`）。

```python
# scripts/query_debug_dump.py
def is_query_debug_enabled() -> bool:
    return (os.getenv("RAG_QUERY_DEBUG_DUMP") or "").strip().lower() in (
        "1", "true", "yes", "on",
    )

def get_query_dump_dir() -> Path:
    directory = get_logs_dir() / "query_dumps"
    directory.mkdir(parents=True, exist_ok=True)
    return directory
```

Web 开发 API：

```python
# scripts/rag_web_server.py
@app.post("/api/dev/query-debug")
async def api_dev_query_debug_set(body: QueryDebugBody):
    patch_env_keys({"RAG_QUERY_DEBUG_DUMP": "1" if body.enabled else "0"})
    ...
```

**7）灌库：同名 PDF 替换旧索引（`rag_pipeline_parse_graph_chat.py`）**

**改动目的（当时）：** 同一 PDF 多次灌库产生多条 `doc_id`（索引记录数 >> 唯一 PDF 数），检索与配图混乱。

```python
# scripts/rag_pipeline_parse_graph_chat.py
async def _remove_existing_docs_for_file(rag, rel: str) -> int:
    """Replace prior index rows that share the same uploaded filename."""
    target = Path(rel).name
    ...
    for doc_id, meta in rows:
        fp = _doc_status_file_path(meta)
        if fp == rel or (fp and Path(fp).name == target):
            matches.append(doc_id)
    ...
    await delete(doc_id)

# _ingest_folder 内每个文件灌入前：
replaced = await _remove_existing_docs_for_file(rag, rel)
if replaced:
    logger.info("INGEST_REPLACE::%s::removed=%d", rel, replaced)
```

**8）灌库图注解析（`processor.py` + `utils.resolve_image_caption`）**

**改动目的（当时）：** MinerU 图注有时在相邻 text 块而非 image 条目本身；统一用 layout 推断，避免 `[图片]` 块 label 为空。

```python
# raganything/processor.py（Plan B 内联 [图片] 块）
caption = resolve_image_caption(items, idx)
footnote = resolve_image_footnote(items, idx)
```

**9）配图 env 门槛（`config/env.example` / `env.example`）**

```bash
RAG_IMAGE_MIN_TERM_LEN=3
RAG_IMAGE_MIN_QUERY_CHARS=6
RAG_IMAGE_MIN_REF_ALIGN=0.28
RAG_QUERY_DEBUG_DUMP=0
```

**10）Web UI 开发面板（`web/index.html` + `app.js` + `styles.css`）**

**改动目的（当时）：** 现场可开关 query dump、查看最近 dump 列表，无需手改 `.env` 重启。

```html
<!-- web/index.html 侧栏「开发」 -->
<input type="checkbox" id="query-debug-dump" />
<label for="query-debug-dump">保存查询调试日志（RAG_QUERY_DEBUG_DUMP）</label>
```

```javascript
// web/static/app.js
const res = await fetch("/api/dev/query-debug", {
  method: "POST",
  headers: { "Content-Type": "application/json" },
  body: JSON.stringify({ enabled: queryDebugDump.checked }),
});
```

### 使用提示（当时）

- 配图 regression：重启 launcher 后，用 Web UI 或 `scripts/test_query_images_webpath.py`（若本地有）验证上述两问。
- 调试 dump：Web「开发」面板开启，或 `RAG_QUERY_DEBUG_DUMP=1`，查看 `logs/query_dumps/`。
- 新机型：改 `config/query_steering_profiles.json`，不必改 `query_doc_steering.py`。
- 重复灌库：同名 PDF 会自动替换；若历史已重复，建议向导「重新灌库（清空后）」一次。

```bash
git show 49e3707
```

---

## `cf18593` · 2026-05-25

**提交说明：** `feat(client): append ingest UX, image gating, and env tuning defaults`

### 本版本目的（当时）

在 Plan B 配图与安装向导初版落地后，现场联调暴露四类问题：**重复灌库**导致索引条数虚高、**闲聊/弱检索仍配图**、**向导灌库 UX 混淆（清空 vs 追加）**、**灌库中断后 UI/知识库半残**。本批改动以「可交付稳定性」为主：配图门槛、增量灌库、env 打包默认值、API Key/401 友好提示、灌库过程互斥与状态同步。

### 概要

| 模块 | 变更 |
|------|------|
| 配图门槛 | `scripts/image_query_refs.py`：闲聊过滤、rerank/词命中率门槛；去掉低分兜底强出图 |
| 灌库 UX | `web/setup.html` + `setup.js`：「追加灌库」vs「重新灌库（清空后）」；问答侧栏 `app.js` 同步文案 |
| 客户端 env | `client_env_manager.py`：`CLIENT_TUNING_KEYS`、先 `env.example` 再 `.env`；`config/env.example` 写入检索/配图默认值 |
| 知识库状态 | `client_setup_service.py`：区分 **唯一 PDF 数** 与 **索引记录条数**（重复灌库提示） |
| Web 服务 | `rag_web_server.py`：灌库中禁止改配置/重载引擎；`ingest_active` 状态；401 友好错误；中断灌库清空 KB |
| 修复 | `rag_web_server.py` 恢复 `get_env_path` 导入（修复 `/api/setup/status` NameError） |
| 文档 | `docs/客户端开发说明.md` 增补「检索/配图高级参数」打包说明；`docs/临时记录.txt` 待办：思考过程英文 |

**规模（提交当时）：** 15 个文件，+747 / −71 行。

### 代码改动

**1）问答配图：检索不够相关则不展示（`image_query_refs.py`）**

**改动目的（当时）：** 用户问「你好」「你弄啥嘞」等闲聊，或知识库无明确命中时，仍展示大量无关手册照片。移除「分数不够也强行选一张图」的兜底。

```python
def query_wants_kb_images(query: str | None) -> bool:
    qn = _normalize_query_for_match(query)
    for pat in _CHITCHAT_ONLY_PATTERNS:
        if re.fullmatch(pat, qn, re.I):
            return False
    ...

def retrieval_supports_images(query, *, retrieved_docs=None, context_text=None) -> bool:
    if not query_wants_kb_images(query):
        return False
    max_score = _max_rerank_score(retrieved_docs)
    if max_score is not None:
        threshold = _image_min_rerank_score()  # 默认跟 MIN_RERANK_SCORE / 0.28
        if max_score < threshold:
            return False
        return True
    # 无 rerank 分：用词命中率 RAG_IMAGE_MIN_TERM_OVERLAP（默认 0.34）
```

```python
# scripts/query_progress_hooks.py — 配图打分带上 rerank 后的 docs
images = images_for_api(docs_text, roots, query=q, retrieved_docs=docs)
```

**2）安装向导：追加灌库 vs 重新灌库（`setup.js` / `setup.html`）**

**改动目的（当时）：** 有旧库时「开始灌库」一律先清空，用户无法**只加新 PDF**。

```javascript
// web/static/setup.js
btnIngest.textContent = hasKb ? "重新灌库（清空后）" : "开始灌库";
btnAppendIngest.textContent = "追加灌库";

if (replaceKb) {
  appendIngestLog(ingestLog, "重新灌库：先清空现有知识库…");
  await clearKnowledgeBase();
} else {
  appendIngestLog(ingestLog, "追加灌库：保留现有知识库，写入新文件…");
}
```

**3）客户端 env：打包默认值 + 向导不冲掉调参项（`client_env_manager.py`）**

**改动目的（当时）：** 安装包以 `config/env.example` 为底稿；向导保存时保留检索/配图调参键。

```python
CLIENT_TUNING_KEYS: frozenset[str] = frozenset({
    "MIN_RERANK_SCORE",
    "RAG_IMAGE_MIN_RERANK_SCORE",
    "RAG_IMAGE_MIN_TERM_OVERLAP",
    "TOP_K", "CHUNK_TOP_K", "MAX_TOTAL_TOKENS",
    ...
})

def apply_env_to_process() -> None:
    load_dotenv(get_env_example_path(), override=False)
    load_dotenv(get_env_path(), override=True)
```

```bash
# config/env.example（打包默认值节选）
MIN_RERANK_SCORE=0.28
RAG_IMAGE_MIN_RERANK_SCORE=0.28
RAG_IMAGE_MIN_TERM_OVERLAP=0.34
```

**4）灌库互斥与 UI 恢复（`rag_web_server.py` + `setup.js`）**

**改动目的（当时）：** 灌库中禁止改配置；停止灌库后同步 `ingest_active`；401 友好提示。

```python
def _reject_if_ingest_active(action: str) -> None:
    if state.ingest_active:
        raise HTTPException(409, f"灌库进行中，无法{action}")

def _friendly_query_error(exc: BaseException) -> str:
    if "401" in str(exc) or "invalid_api_key" in str(exc).lower():
        return "LLM API Key 无效或与 API 地址不匹配（401）。…"
```

```python
# /api/setup/status 响应字段
status["ingest_active"] = state.ingest_active
status["ingest_cancel_requested"] = state.ingest_cancel_requested
```

**5）知识库计数：唯一 PDF vs 索引记录（`client_setup_service.py`）**

**改动目的（当时）：** 同一 PDF 多次灌库后，索引条数虚高，需区分唯一文件名。

```python
def _unique_doc_paths(full_docs_path: Path) -> int:
    names: set[str] = set()
    for item in data.values():
        fp = str(item.get("file_path") or "")
        if fp:
            names.add(Path(fp).name)
    return len(names)

# 状态文案：已灌库 7 个 PDF（索引记录 23 条，含重复灌库）
```

### 使用提示（当时）

- **开发调试**仍主要改仓库根目录 `.env`；**打包交付**在 `config/env.example` 调好再 `pack_client.ps1`
- 配图门槛 env：`RAG_IMAGE_MIN_RERANK_SCORE`、`RAG_IMAGE_MIN_TERM_OVERLAP`（见 `env.example` 注释）
- 重复灌库后若检索异常：向导 **重新灌库（清空后）** 只灌一次，确认唯一 PDF 数正常

```bash
git show cf18593
```

---

## `7f1f125` · 2026-05-24

**提交说明：** `feat(rag): add Plan B image refs, bbox ingest links, and multi-manual answer steering`

### 本版本目的（当时）

文本灌库（跳过多模态 LLM）时 PDF 内图片无法被问答展示；多手册并存时 LLM 把不同设备保养步骤**合并成自编分类**（如「常规/深度润滑」）；传动丝杆等跨手册问题配图**张冠李戴**。本版本实现 **Plan B**：灌库写入轻量 `[图片]` 索引块 + MinerU bbox 关联；问答时从检索 context 解析配图并严格按设备/问题打分；注入 **answer fidelity** 提示约束分设备作答。

### 概要

| 模块 | 变更 |
|------|------|
| 灌库索引 | `raganything/utils.py`：`context_text_for_image()` 同页 bbox 最近文字；`build_image_ref_block()` |
| 灌库流程 | `raganything/processor.py`：`_build_text_with_inline_image_refs()` 文档顺序嵌入 `[图片]` 块 |
| 问答配图 | `scripts/image_query_refs.py`（新）：解析检索 context、bbox 补图、按查询打分 |
| 问答 hook | `scripts/query_progress_hooks.py`：rerank 后 SSE 推送 `related_images` |
| 多手册作答 | `scripts/query_doc_steering.py`：`build_answer_fidelity_user_prompt()` |
| 客户端 | 视觉模型表单项、多模态开关、灌库进度 UI 增强 |
| Web | 问答页展示参考资料图片；setup 向导步骤 3 完善 |

**规模（提交当时）：** 19 个文件，约 +1718 / −90 行。

### 代码改动

**1）Plan B 灌库（`processor.py` + `utils.py`）**

**改动目的（当时）：** 跳过多模态 LLM 时图片不进索引；在文本灌库路径写入轻量 `[图片]` 块，含 bbox 邻近 **关联正文**，与正文按 `content_list` 顺序交错进 chunk。

```python
# raganything/utils.py
def build_image_ref_block(*, img_path, page_idx=None, caption="", footnote="", context="") -> str:
    lines = ["[图片]", f"图片路径：{img_path}"]
    if page_idx is not None:
        lines.append(f"页码：{page_idx}")
    if caption:
        lines.append(f"图注：{caption}")
    if context:
        lines.append(f"关联正文：{context[:800]}")
    return "\n".join(lines)
```

```python
# raganything/processor.py — 文档顺序嵌入
text_content = self._build_text_with_inline_image_refs(normalized_content_list, ...)
```

**2）问答配图（`image_query_refs.py` + `query_progress_hooks.py`）**

**改动目的（当时）：** 从检索 context 解析 `[图片]`；`content_list.json` bbox 补图；按 query 打分，每台手册最多 1 张，上限 4 张，SSE 推送。

```python
# scripts/image_query_refs.py
refs = extract_image_refs_from_context(retrieved_text)
refs = supplement_refs_from_content_lists(refs, media_roots, anchor_text=...)
images = images_for_api(docs_text, media_roots, query=query, retrieved_docs=docs, limit=4)
```

```python
# scripts/query_progress_hooks.py（rerank 后）
await _emit_related_images(retrieved_docs)
# SSE: {"type": "related_images", "images": [...]}
```

**3）多手册回答忠实度（`query_doc_steering.py`）**

**改动目的（当时）：** LLM 把多手册保养合并成自编「常规/深度润滑」；注入 fidelity 提示，要求按来源分节、保留原文型号周期。

```python
def build_answer_fidelity_user_prompt(query: str) -> str:
    parts = [
        "回答须忠实于检索到的各文档原文，不得自行归纳或重组：",
        "若内容来自多份不同设备、产品或手册，必须按来源分别列出；",
        "不得把不同来源的周期、型号、操作步骤合并成自编分类或「通用流程」；",
    ]
    return " ".join(parts)
```

```python
# scripts/rag_pipeline_parse_graph_chat.py — 并入 aquery
steer = build_user_prompt_for_query(query)  # fidelity + profile steering
```

### 使用提示（当时）

- 配图需重灌后 chunk 含 `[图片]` 块
- 验证：「传动丝杆怎么保养」→ 分设备答案 + 配图与手册一致

```bash
git show 7f1f125
```

---

## `d1da3c0` · 2026-05-24

**提交说明：** `feat(client): add offline pack script and thin NanxingRAG.exe launcher`

### 本版本目的（当时）

补齐绿色包 **打包脚本**、**PyInstaller 薄启动器 exe**、**离线 tiktoken** 与 **`config/env.example`**，客户解压即用，无需 `uv`/命令行。

### 概要

| 模块 | 变更 |
|------|------|
| 打包 | `scripts/pack_client.ps1`、`pack_client.bat` |
| 启动器 | `scripts/client_launcher_exe.py` → `NanxingRAG.exe` |
| 模板 | `config/env.example`、`docs/client/README.txt` |
| 路径 | `client_paths.py`：`get_tiktoken_cache_dir()` |

**规模（提交当时）：** 8 个文件，约 +495 / −7 行。

### 代码改动

**1）打包脚本（`scripts/pack_client.ps1`）**

**改动目的（当时）：** 一键复制 `config/`、`web/`、`.venv`、内置模型等到 `dist/NanxingRAG/`，生成可分发绿色包。

```powershell
# scripts/pack_client.ps1（节选）
Copy-Item -Recurse config, web, scripts, raganything $OutDir
# 生成 NanxingRAG.exe（见 client_launcher_exe.py）
```

**2）薄启动器 exe（`scripts/client_launcher_exe.py`）**

**改动目的（当时）：** 客户双击 `NanxingRAG.exe` 即拉起内嵌 Python + `client_launcher.py`，无需安装 uv。

```python
# PyInstaller 入口：设置 RAG_CLIENT_MODE=1、RAG_CLIENT_APP_ROOT 后 exec client_launcher
```

**3）离线 tiktoken（`client_paths.py`）**

**改动目的（当时）：** 无网环境 LightRAG 分词仍可用；缓存目录随包放在 `config/tiktoken_cache`。

```python
def get_tiktoken_cache_dir() -> Path:
    d = get_config_dir() / "tiktoken_cache"
    d.mkdir(parents=True, exist_ok=True)
    return d
```

```bash
# config/env.example
TIKTOKEN_CACHE_DIR=config/tiktoken_cache
```

```bash
git show d1da3c0
```

---

## `1c7759f` · 2026-05-24

**提交说明：** `feat(client): add setup wizard, streaming ingest, and portable launcher`

### 本版本目的（当时）

增加 **浏览器安装向导（/setup）**、**SSE 流式可取消灌库**、**config/ + data/ 客户端布局** 与 **`client_launcher.py`**。

### 概要

| 模块 | 变更 |
|------|------|
| 布局 | `client_paths.py`、`client_env_manager.py`、`client_setup_service.py` |
| 向导 | `web/setup.html`、`setup.js`、`setup.css`；`/api/setup/*` |
| 灌库 | `ingest_ui.js`；`/api/ingest/stream`、取消灌库 |
| 启动 | `client_launcher.py`；未完成 setup 打开 `/setup` |
| 依赖 | `opencv-python-headless`（MinerU） |
| 文档 | `docs/使用说明.md`、`docs/客户端开发说明.md`（初版） |

**规模（提交当时）：** 20 个文件，约 +2979 / −70 行。

### 代码改动

**1）客户端目录布局（`client_paths.py`）**

**改动目的（当时）：** 绿色包内 `config/`（env）、`data/`（rag_storage、pipeline_parse、models）与开发仓库根目录解耦。

```python
def get_config_dir() -> Path:
    return get_app_root() / "config"

def get_rag_storage_dir() -> Path:
    return get_data_dir() / "rag_storage"
```

**2）安装向导 API（`client_setup_service.py` + `rag_web_server.py`）**

**改动目的（当时）：** 浏览器 `/setup` 四步向导；未完成 setup 时 launcher 自动打开向导而非问答页。

```python
@app.get("/api/setup/status")
@app.post("/api/setup/env")
@app.post("/api/setup/reload-rag")
```

```python
# scripts/client_launcher.py（节选）
if not is_setup_complete():
    webbrowser.open(f"http://127.0.0.1:{port}/setup")
```

**3）SSE 流式灌库（`rag_web_server.py` + `ingest_ui.js`）**

**改动目的（当时）：** 上传 PDF 后实时进度；用户可停止灌库，服务端清空半残知识库。

```python
@app.post("/api/ingest/stream")
async def api_ingest_stream(files: list[UploadFile] = File(...)):
    return StreamingResponse(_ingest_stream_events(files), media_type="text/event-stream")
```

```javascript
const res = await fetch("/api/ingest/stream", { method: "POST", body: fd });
// 解析 ingest_saved / progress / done / cancelled
```

**4）setup 完成判定（`client_paths.py`）**

```python
def is_setup_complete() -> bool:
    marker = get_data_dir() / ".setup_complete.json"
    return marker.is_file() and json.loads(marker.read_text()).get("version") == _SETUP_MARKER_VERSION
```

```bash
git show 1c7759f
```

---

## `2bb64e8` · 2026-05-19

**提交说明：** `docs: add version changelog with recent web UI release notes`

### 本版本目的（当时）

新增本文档，回填 `f53193a` / `905fcca` / `dba2413` 等 Web 相关版本的详细条目。

**规模（提交当时）：** 1 个文件，+621 行。

### 代码改动

**1）建立本文档结构与维护约定**

**改动目的（当时）：** 后续 Web/客户端版本有统一「版本号 = 短 hash + 改动目的 + 代码节选」模板，避免只有 commit message 无上下文。

```markdown
## `<hash>` · YYYY-MM-DD
**提交说明：** `type(scope): …`
### 本版本目的（当时）
### 概要
### 代码改动
**改动目的（当时）：** …
\`\`\`python
# 关键片段
\`\`\`
```

```bash
git show 2bb64e8
```

---

## `f53193a` · 2026-05-19

**提交说明：** `docs(web): expand web UI guide with API and query flow walkthrough`

### 本版本目的（当时）

Web 界面与后端已在 `dba2413` / `905fcca` 落地，但缺少**面向非前端开发者**的系统说明：接口在哪注册、点击「发送」后代码如何从前端一路走到 LightRAG、各 SSE 事件含义等。本版本在 [`网页问答界面.md`](网页问答界面.md) 中补充**学习向**长文档，便于日后自学与交接。

### 概要

| 模块 | 变更 |
|------|------|
| 文档 | `docs/网页问答界面.md`：大幅扩充前后端连接、接口注册、SSE 协议、问答全链路逐步说明 |
| 时序图 | Mermaid `sequenceDiagram`（浏览器 → FastAPI → hooks → LightRAG → LLM） |
| 对照表 | 接口/装饰器/函数名对照、SSE type 与前端 UI 对照、阶段与文件对照 |

**规模（提交当时）：** 1 个文件，约 +483 / −5 行。

### 文档新增章节（节选）

**1）「前后端连接与接口说明」**

- 同域单体架构（FastAPI 同时提供 `/`、`/static/*`、`/api/*`）
- `lifespan` 启动时 `_build_rag` → `state.rag`
- 接口一览表 + **「接口在代码里怎么注册」**（`@app.get` / `@app.post` 与函数名对照）
- `app.mount("/static", ...)` 静态资源机制
- `_load_rpc()` 与 `rag_pipeline_parse_graph_chat.py` 分工

**2）各 API 详细说明**

- `GET /api/health`：响应 JSON 示例、15 秒轮询、`ready` 与发送按钮联动
- `POST /api/query/stream`：请求体、`text/event-stream` 响应格式、SSE 事件类型表
- 后端内部链：`query_progress_hooks` → `aquery` → `StreamCotParser` → `query_doc_steering`
- `POST /api/ingest`：FormData 上传与 `_ingest_folder` 流程

**3）「点击『发送』后的完整代码路径（逐步学习）」**

按第 0–8 步展开：服务启动 → 前端 submit → `fetch` → `api_query_stream` → `_query_stream_events` worker → `_run_aquery` → LightRAG mix 内部阶段 → SSE 推送 → 前端 `handleEvent` 更新 UI。

**改动目的（当时）：** 文档内用 SSE 事件表对照前端分支，读者不必翻 `app.js` 猜 `type` 含义。

```javascript
// docs/网页问答界面.md 中引用的 SSE type → 前端处理（与 app.js handleEvent 一致）
// status          → setLoadingStatus
// retrieval_scope → showRetrievalScope
// thinking_delta  → appendThinkingDelta（可折叠思考区）
// answer_delta    → appendAnswerDelta（Markdown 回答区）
// related_images  → showRelatedImages
// done            → 结束流式、滚到底
// error           → 抛错提示
```

**4）文档中引用的接口注册示例（与 `rag_web_server.py` 一致）**

**改动目的（当时）：** 读者对照文档即可在代码里定位同名函数，而非只看 prose 描述。

```python
# scripts/rag_web_server.py
app = FastAPI(lifespan=lifespan)
app.mount("/static", StaticFiles(directory=_WEB_DIR / "static"), name="static")

@app.get("/")
@app.get("/api/health")
@app.post("/api/query/stream")
@app.post("/api/ingest")
```

### 使用提示（当时）

阅读顺序建议：安装/启动 → 前后端连接 → 点击发送全链路 → Mermaid 时序图。需渲染 Mermaid 时可安装 VS Code/Cursor 插件 **Markdown Preview Mermaid Support**。

查看完整差异：

```bash
git show f53193a
```

---

## `905fcca` · 2026-05-19

**提交说明：** `feat(web): rebrand UI with light theme and Nanxing branding`

### 本版本目的（当时）

`dba2413` 的 Web UI 采用暗色主题与「RAG-Anything」品牌，不符合南兴现场使用场景。本版本改为**明亮风格**、**更大字号**、**南兴 logo 与产品名**，并将侧栏「LightRAG 模式」简化为「RAG模式」；**不改后端接口与 RAG 逻辑**。

### 概要

| 模块 | 变更 |
|------|------|
| 页面 | `web/index.html`：标题改为「南兴知识库问答助手」；`<title>` 同步；侧栏 logo 换为 `<img>` |
| 样式 | `web/static/styles.css`：浅色 `:root` 变量、南兴绿主色 `#2e9e4f`、字号整体上调 |
| 资源 | `web/static/images/nanxing-logo.png`：南兴 logo |
| 文案 | `web/static/app.js`：欢迎系统消息改为南兴知识库问答助手 |

**规模（提交当时）：** 4 个文件，约 +107 / −172 行（HTML 去冗余空行，CSS 重写配色）。

### 代码改动

**1）品牌与标题（`index.html`）**

**改动目的（当时）：** 现场识别度；去掉通用 RAG-Anything 名称。

```html
<h1>南兴知识库问答助手</h1>
<img class="brand-logo" src="/static/images/nanxing-logo.png" alt="南兴装备" />
<label for="query-mode">RAG模式</label>
```

**2）明亮主题（`styles.css`）**

**改动目的（当时）：** 工厂/办公环境更易阅读；与南兴绿色品牌一致。

```css
:root {
  --bg: #f0f2f5;
  --text: #1f2937;
  --accent: #2e9e4f;
  --user-bubble: #e3f2fd;
  --bot-bubble: #ffffff;
  --font-size-base: 1.0625rem;
}
.btn.primary {
  color: #ffffff;
}
```

**3）欢迎语（`app.js`）**

```javascript
appendMessage("system", "欢迎使用南兴知识库问答助手。左侧可查看工作目录与模式，下方输入问题开始对话。");
```

### 使用提示（当时）

仅前端静态资源变更，**无需**改 `.env` 或重启灌库；刷新浏览器（必要时 Ctrl+F5）即可。后端 `rag_web_server.py` 与 API 路径不变。

查看完整差异：

```bash
git show 905fcca
```

---

## `dba2413` · 2026-05-18

**提交说明：** `feat(web): add browser Q&A UI with doc steering and streaming UX`

### 本版本目的（当时）

在**不改 CLI 灌库/问答主流程**的前提下，增加**浏览器图形界面**，并解决多本封边机手册混库时的两类问题：

1. **跨机型串台**：用户写明「高速智能封边机」时，仍会召回《自动封边机…》等 chunk，或知识图谱里合并的「输送链条 + 每天长城导轨油」描述，导致答案错误。
2. **展示体验**：流式输出需区分**思考过程**与**正式回答**、支持 **Markdown** 排版；对话区存在滚动不到底、界面卡在旧消息等问题。

本版本用 **机型规则（代码内）+ 可见检索范围提示 + KG 上下文清洗** 处理串台，用 **SSE 分事件 + 前端布局修复** 处理展示；**不**要求为每台机器单独写 `.env`。

### 概要

| 模块 | 新增/变更 |
|------|-----------|
| Web 服务 | `scripts/rag_web_server.py`：FastAPI、`/api/query/stream`（SSE）、`/api/ingest`、健康检查 |
| 前端 | `web/`：对话、侧栏状态、上传灌库；离线 `marked` + `DOMPurify`（`web/static/vendor/`） |
| 机型过滤 | `scripts/query_doc_steering.py`：按问题识别机型，rerank 后过滤无关 PDF；清洗 mix 模式 KG 冲突行 |
| 流式解析 | `scripts/stream_cot_parser.py`：`think` / `redacted_reasoning` 标签 → `thinking_delta` / `answer_delta` |
| 进度钩子 | `scripts/query_progress_hooks.py`：检索 / 重排 / 生成真实阶段提示 + `retrieval_scope` 事件 |
| 缓存工具 | `scripts/clear_query_llm_cache.py`：仅清除 `query` 类 LLM 缓存（保留灌库 extract 缓存） |
| 流水线 | `_build_rag` 时 `install_query_steering_hooks()`；`_query_extras_from_env(query)` 按问题注入引导 |
| 依赖 | `pyproject.toml` 增加 optional extra **`web`**（fastapi、uvicorn、sentence-transformers 等） |
| 文档 | `docs/网页问答界面.md`、`docs/新增PDF与机型检索规则说明.md`；脚本说明增加 Web 章节 |

**规模（提交当时）：** 17 个文件，约 +2989 / −8 行。

### 代码改动

**1）Web 服务：复用 pipeline 的 `_build_rag`，流式 SSE 问答**

**改动目的（当时）：** 浏览器与命令行共用同一套 `.env`、同一 `-w` 工作目录；避免维护两套 RAG 配置。流式接口在检索/重排/生成阶段推送**真实状态**，LLM 阶段推送思考与回答分通道增量。

```python
# scripts/rag_web_server.py（lifespan 片段）
rag, config, _logger = await rpc._build_rag(
    wd, pod, skip_multimodal=skip_mm
)
# ...
async def _run_aquery(q: str, mode: str, *, stream: bool):
    return await state.rag.aquery(
        q, mode=mode, stream=stream, vlm_enhanced=False,
        **rpc._query_extras_from_env(q),
    )
```

SSE 事件类型包括：`status`（检索/重排/生成）、`retrieval_scope`（机型过滤说明）、`thinking_delta`、`answer_delta`、`done` / `error`。

**2）`query_doc_steering.py`：机型 profile + rerank 后过滤 + KG 清洗**

**改动目的（当时）：** 多本手册都含「输送链条」等词，向量/图谱会混用。规则写在 `MACHINE_PROFILES`（高速智能 / 高速自动 / 双端 / 自动封边机等），**问题里出现机型短语即自动生效**，无需每台机器配 `.env`。过滤后通过 SSE 在界面展示「检索范围 / 已排除手册」，避免暗箱操作。

```python
# scripts/query_doc_steering.py（内置 profile 示例）
{
    "id": "high_speed_smart",
    "label": "高速智能封边机",
    "query_phrases": ["高速智能封边机", "高速智能", "NB9-Smart", "NB10-Smart"],
    "deny_path_substrings": [
        "自动封边机维护保养手册",
        "高速自动封边机维护保养手册",
        # ...
    ],
}
```

```python
# 挂到 LightRAG：rerank 之后过滤 chunk；构建上下文之后清洗 KG 文本
def install_query_steering_hooks() -> None:
    install_doc_filter_on_rerank()
    install_kg_context_scrub()  # patch _build_query_context
```

针对「高速智能 + 输送链条」等场景，还会生成**按问题**的 `user_prompt` 片段（与 chunk 冲突时以正文为准），由 `_query_extras_from_env(query)` 并入 `aquery`。

**3）`stream_cot_parser.py`：流式拆分思考与回答**

**改动目的（当时）：** 模型常输出 `think` / `redacted_reasoning` 包裹的思考块。原先整段当答案展示，用户难以阅读。增量解析标签（可跨 token 块断裂），Web 侧 `feed()` 后分别 SSE 推送 `thinking_delta` / `answer_delta`。

```python
# scripts/stream_cot_parser.py
_COT_TAGS = (
    ("think", "think"),
    ("redacted_reasoning", "redacted_reasoning"),
)

@dataclass
class StreamCotParser:
    def feed(self, chunk: str) -> list[tuple[Kind, str]]:
        self._buf += chunk
        # 跨 token 断裂时保留尾部不完整标签，返回 ("thinking"|"answer", text_piece)
        ...
```

```python
# scripts/rag_web_server.py（流式 worker 片段）
cot_parser = StreamCotParser()
for ev_kind, piece in cot_parser.feed(token):
    if ev_kind == "thinking":
        yield _sse({"type": "thinking_delta", "text": piece})
    else:
        yield _sse({"type": "answer_delta", "text": piece})
```

**4）前端：Markdown、检索范围条、滚动修复**

**改动目的（当时）：** 离线环境不能依赖 CDN；`web/static/vendor/` 内置 marked、DOMPurify。对话列表底部锚点 + `requestAnimationFrame` 滚到底；用户上滑阅读历史时不强行拉回（`scrollPinnedToBottom`）。

```javascript
// web/static/app.js
let scrollPinnedToBottom = true;

function scrollMessages(force = false) {
  const gap = messagesEl.scrollHeight - messagesEl.scrollTop - messagesEl.clientHeight;
  if (!force && !scrollPinnedToBottom && gap > 80) return;
  requestAnimationFrame(() => {
    getMessagesEnd()?.scrollIntoView({ block: "end", behavior: "instant" });
    messagesEl.scrollTop = messagesEl.scrollHeight;
  });
}

function renderMarkdown(el, md) {
  const html = marked.parse(md);
  el.innerHTML = DOMPurify.sanitize(html);
}
```

```javascript
// handleEvent 节选
if (ev.type === "retrieval_scope") showRetrievalScope(ui, ev);
if (ev.type === "thinking_delta") appendThinkingDelta(ui, ev.text);
if (ev.type === "answer_delta") appendAnswerDelta(ui, ev.text);
```

**5）`clear_query_llm_cache.py`**

**改动目的（当时）：** 调试检索/机型规则后，若 `ENABLE_LLM_CACHE=true`，可能仍命中旧问答缓存。本脚本**只删** `cache_type=query` 或 key 为 `default:query:*` 的条目，**不删**灌库用的 `extract`/`keywords` 缓存。

```python
# scripts/clear_query_llm_cache.py
def _is_query_entry(key: str, value: object) -> bool:
    if key.startswith("default:query:"):
        return True
    return isinstance(value, dict) and value.get("cache_type") == "query"
```

```bash
uv run python scripts/clear_query_llm_cache.py -w rag_storage_run
```

**6）`env.example` 补充**

**改动目的（当时）：** 记录 Web/机型过滤开关与 mix token 预算调参起点（与 `df5a2ff` 调试结论衔接）。

```bash
RAG_QUERY_DOC_FILTER=true
RAG_QUERY_KG_SCRUB=true
RAG_WEB_WORKING_DIR=rag_storage_run
ENABLE_LLM_CACHE=true
MAX_TOTAL_TOKENS=52000
```

### 使用提示（当时）

```bash
uv sync --extra web --extra local-embed
$env:RAG_WEB_WORKING_DIR = "rag_storage_run"
uv run python scripts/rag_web_server.py
# http://127.0.0.1:8765
```

新增 PDF、维护机型规则见 [`新增PDF与机型检索规则说明.md`](新增PDF与机型检索规则说明.md)。

查看完整差异：

```bash
git show dba2413
```

---

## `df5a2ff` · 2026-05-18

**提交说明：** `feat(rag): retrieval debug tools and mix token budget notes`

### 本版本目的（当时）

定位「目标 chunk 在向量召回里存在，但**未进入最终送给 LLM 的上下文**」一类问题（典型用例：`chunk-0d30`、电控板保养周期）。需要工具看清 **merge 后 / rerank 后 / token 截断后** 各阶段 chunk 列表，并在 `env.example` 中记录 **mix 模式 token 预算**调参结论。

### 概要

- 新增 `scripts/capture_merged_chunk_ids.py`：hook `_merge_all_chunks`，把 **rerank 前** 合并 chunk 的 id 写入 `docs/merged_pre_rerank_chunk_ids.txt`。
- 增强 `scripts/dump_query_context.py`：支持 `--markers` 在 chunk 正文中搜关键字；支持 `DUMP_QUERY_DEFAULT`。
- `rag_pipeline_parse_graph_chat.py`：`ENABLE_LLM_CACHE` 从 `.env` 读取（不再写死 `True`）。
- 文档：`docs/chunk-0d30-检索调试记录.md`；`env.example` 补充 `MAX_TOTAL_TOKENS=52000` 等 mix 典型值说明。

**规模（提交当时）：** 5 个文件，约 +284 / −35 行。

### 代码改动

**1）`capture_merged_chunk_ids.py`**

**改动目的（当时）：** `dump_query_context` 看到的是截断后的 final chunks；要判断某 id 是否在 **rerank 前约 30 路合并结果** 里，需在 `_merge_all_chunks` 出口打点。

```python
# scripts/capture_merged_chunk_ids.py
def _install_merge_hook(out: Path) -> None:
    import lightrag.operate as op
    _orig = op._merge_all_chunks

    async def _wrapped(*args, **kwargs):
        merged = await _orig(*args, **kwargs)
        lines = [str(c.get("chunk_id") or c.get("id") or "") for c in merged]
        out.write_text("\n".join(lines) + "\n", encoding="utf-8")
        return merged

    op._merge_all_chunks = _wrapped
```

```bash
uv run python scripts/capture_merged_chunk_ids.py -w ./rag_storage_run "你的测试问题"
# 输出 docs/merged_pre_rerank_chunk_ids.txt
```

**2）`dump_query_context.py`：`--markers` 与 `DUMP_QUERY_DEFAULT`**

**改动目的（当时）：** 在 chunk 正文里搜关键字（如 `3.14.5`、电控板），快速判断目标 id 是否进入最终上下文；省略 QUERY 时可读 env 默认问句。

```python
# scripts/dump_query_context.py
query = (args.query or "").strip() or (os.getenv("DUMP_QUERY_DEFAULT") or "").strip()
markers = _split_markers(args.markers)
# 对每个 chunk preview 检查 marker 子串命中
```

```bash
uv run python scripts/dump_query_context.py -w ./rag_storage_run \
  --markers "3.14.5,每季度,电控板" \
  "四种封边机电控板保养周期分别是多少"
```

**3）`ENABLE_LLM_CACHE` 从 env 读取（`rag_pipeline_parse_graph_chat.py`）**

**改动目的（当时）：** 原先写死 `enable_llm_cache=True`，调试检索规则后无法通过 `.env` 关闭问答缓存。

```python
enable_llm_cache = os.getenv("ENABLE_LLM_CACHE", "true").strip().lower() in ("1", "true", "yes")
lightrag = LightRAG(..., enable_llm_cache=enable_llm_cache, ...)
```

**4）mix 模式 token 预算（`env.example` + `docs/chunk-0d30-检索调试记录.md`）**

**改动目的（当时）：** `MAX_TOTAL_TOKENS` 过低时，KG 实体/关系 JSON 占满预算，**rerank 后的 chunk 按顺序被 `truncate_list_by_token_size` 裁掉**，出现「召回有、最终上下文无」。将 workable 下限记在 `env.example`（**52000**）。

```bash
# env.example（mix 典型值节选）
MAX_TOTAL_TOKENS=52000
MIN_RERANK_SCORE=0.22
TOP_K=20
CHUNK_TOP_K=30
```

查看完整差异：

```bash
git show df5a2ff
```

---

## `a2995b3` · 2026-05-14

**提交说明：** `feat(rag): query steering env, rerank helper, and docs`

### 本版本目的（当时）

在**不反复改脚本**的前提下，增强两件事：一是**问答阶段可调**（用 `.env` 引导检索与生成、并接上 rerank）；二是**排障与文档**（导出检索上下文、补操作说明）。另外修正「纯文本优先灌库」路径下**表格内容丢失**的问题。

### 概要

- 从环境变量读取查询引导字段，经 `rag.aquery(..., **kwargs)` 传入 LightRAG `QueryParam`。
- 新增 `raganything/pipeline_rerank.py`，按环境组装 `rerank_model_func` 并挂到 `LightRAG`。
- 新增 `scripts/dump_query_context.py`，仅导出结构化检索上下文、不走 LLM。
- `processor`：在 `skip_multimodal_processing` 时把 MinerU 表格块拼进纯文本，避免表格不进索引。
- 文档：`docs/model-download-ingest-query.md`、`docs/文档解析灌库与图谱问答脚本说明.md`、`env.example`、README 等。

**规模（提交当时）：** 10 个文件，约 +814 / −12 行。

### 代码改动

**1）主脚本：导入前尊重嵌入离线，避免过早访问 Hub**

**改动目的（当时）：** 在 `import lightrag`（进而可能拉 `transformers`）之前就打开 Hub 离线开关，避免仅因**启动/查询**就访问 `huggingface.co`；与「向量模型已缓存、本机离线跑」的用法一致。

```python
# scripts/rag_pipeline_parse_graph_chat.py
load_dotenv(dotenv_path=_ROOT / ".env", override=False)

# Before heavy imports (e.g. lightrag → transformers), honor embed-offline for hub.
if (os.getenv("HF_EMBED_OFFLINE") or "").strip().lower() in ("1", "true", "yes"):
    os.environ["HF_HUB_OFFLINE"] = "1"
    os.environ.setdefault("TRANSFORMERS_OFFLINE", "1")
```

**2）从 `.env` 注入 `QueryParam`：`user_prompt` / `hl_keywords` / `ll_keywords`**

**改动目的（当时）：** 把「每次查询都想带的约束」从代码里挪到环境变量：例如减轻**跨文档串题**、强调只依据某类手册作答，或固定一组高/低层关键词参与 LightRAG 检索，**改 `.env` 即可试参**，无需改 `aquery` 调用处。

```python
# scripts/rag_pipeline_parse_graph_chat.py
def _split_env_csv(name: str) -> list[str]:
    raw = (os.getenv(name) or "").strip()
    if not raw:
        return []
    return [x.strip() for x in raw.split(",") if x.strip()]


def _query_extras_from_env() -> dict:
    """Optional ``QueryParam`` fields from ``.env`` (retrieval / answer steering)."""
    out: dict = {}
    up = (os.getenv("RAG_QUERY_USER_PROMPT") or "").strip()
    if up:
        out["user_prompt"] = up
    hk = _split_env_csv("RAG_QUERY_HL_KEYWORDS")
    if hk:
        out["hl_keywords"] = hk
    lk = _split_env_csv("RAG_QUERY_LL_KEYWORDS")
    if lk:
        out["ll_keywords"] = lk
    return out
```

交互问答里调用示例：

```python
ans = await rag.aquery(
    q, mode=query_mode, vlm_enhanced=False, **_query_extras_from_env()
)
```

**3）构建 `LightRAG` 时挂上环境驱动的 rerank**

**改动目的（当时）：** LightRAG 只有在实例上设置了 `rerank_model_func` 且查询参数开启 rerank 时才会真正重排；此前若只配环境没有函数，会静默跳过。此处把 **HF / Jina / Cohere 等** 的组装集中成一处，与 `MIN_RERANK_SCORE`、`RERANK_BY_DEFAULT` 等一起构成**可配置的精排链路**。

```python
# scripts/rag_pipeline_parse_graph_chat.py
from raganything.pipeline_rerank import build_rerank_model_func_from_env

rerank_model_func = build_rerank_model_func_from_env()

lightrag = LightRAG(
    working_dir=str(working_dir),
    llm_model_func=llm_model_func,
    embedding_func=embedding_func,
    rerank_model_func=rerank_model_func,
    enable_llm_cache=True,
    embedding_func_max_async=int(os.getenv("EMBEDDING_FUNC_MAX_ASYNC", "1")),
    embedding_batch_num=int(os.getenv("EMBEDDING_BATCH_NUM", "1")),
)
```

**4）`build_rerank_model_func_from_env`：按 `RERANK_BINDING` 分支（HF / Jina / Cohere / 阿里云等）**

**改动目的（当时）：** 把 rerank 后端选择与密钥、模型名、离线加载（CrossEncoder 本地快照）等**统一封装**，脚本只调一个工厂函数；关闭 rerank 时返回 `None`，避免各处重复 `if os.getenv(...)`。

```python
# raganything/pipeline_rerank.py
def build_rerank_model_func_from_env() -> Callable[..., Any] | None:
    """Return a partial rerank coroutine, or ``None`` if reranking is disabled."""
    binding = (os.getenv("RERANK_BINDING") or "").strip().lower()
    if binding in ("", "none", "off", "false", "0", "disabled"):
        return None

    model = (os.getenv("RERANK_MODEL") or "").strip()
    api_key = (os.getenv("RERANK_BINDING_API_KEY") or "").strip() or None
    base_url = (os.getenv("RERANK_BINDING_HOST") or "").strip() or None

    if binding in ("hf", "local", "cross_encoder", "sentence_transformers"):
        mid = model or "BAAI/bge-reranker-base"
        return partial(hf_cross_encoder_rerank, model=mid)
    # ... jina / cohere / aliyun ...
```

**5）跳过多模态时合并表格文本进 `text_content`**

**改动目的（当时）：** `separate_content` 只合并 `type == "text"`，表格进 `multimodal_items`。在 **`skip_multimodal_processing=True`**（文本优先灌库）时多模态阶段不再插入，导致**表格从未进入向量/图谱文本通道**。补上表格正文/HTML，避免「整页只有表」的文档**完全不可检索**。

```python
# raganything/processor.py
def _flatten_table_text_for_skip_multimodal(
    self, items: List[Dict[str, Any]]
) -> str:
    """Collect table bodies for text-only ingest when multimodal stage is skipped."""
    parts: List[str] = []
    for item in items:
        if not isinstance(item, dict) or item.get("type") != "table":
            continue
        body = item.get("table_body")
        if isinstance(body, str) and body.strip():
            parts.append(body.strip())
            continue
        content = item.get("content")
        if isinstance(content, dict):
            html = (content.get("html") or "").strip()
            if html:
                parts.append(html)
    if not parts:
        return ""
    return "\n\n".join(f"[Table]\n{t}" for t in parts)
```

```python
# raganything/processor.py（insert_content_list 流程片段）
if skip_multimodal_processing:
    table_blob = self._flatten_table_text_for_skip_multimodal(
        normalized_content_list
    )
    if table_blob:
        if text_content.strip():
            text_content = text_content.strip() + "\n\n" + table_blob
        else:
            text_content = table_blob
        self.logger.info(
            "skip_multimodal_processing: appended %d chars of table text for indexing",
            len(table_blob),
        )
```

**6）`dump_query_context.py`**

**改动目的（当时）：** 调试「检索到了什么」时，若每次都走完整 `aquery` 会**调用 LLM、成本高且难对比**。该脚本走 `aquery_data` 同类路径，只打印实体/关系/分块等结构化结果。

```python
# scripts/dump_query_context.py（启动与查询路径）
load_dotenv(_ROOT / ".env", override=False)
if (os.getenv("HF_EMBED_OFFLINE") or "").strip().lower() in ("1", "true", "yes"):
    os.environ["HF_HUB_OFFLINE"] = "1"

rag, config, _ = await rpc._build_rag(working_dir, parser_output_dir, skip_multimodal=...)
data = await rag.aquery_data(query, param=QueryParam(mode=args.query_mode))
chunks = data.get("chunks") or data.get("text_chunks") or []
```

```bash
uv run python scripts/dump_query_context.py -w ./rag_storage_run "传动丝杆怎么保养"
```

查看完整差异：

```bash
git show a2995b3
```

---

## `fa0ed92` · 2026-05-12

**提交说明：** `Pin CUDA PyTorch in uv lock; improve HF embed offline and CLI UX`

### 本版本目的（当时）

解决 **Windows + `uv sync`** 下容易装到 **CPU 版 torch**、与本地 CUDA 不匹配的问题；让 **HF 向量模型**在离线、仅本地缓存时也能稳定加载；并改善 **命令行交互**（误触空行退出、Ctrl+C 等），补中文流水线说明降低上手成本。

### 概要

- `pyproject.toml` + `uv.lock`：为 Windows + uv 场景固定从 **PyTorch cu130** 索引解析 `torch` / `torchvision`，避免误装 PyPI 上的小型 **CPU-only** 轮子。
- `local_hf_embedding`：自动设备（CUDA / MPS / CPU）、离线标志、Hub 缓存快照路径、`HF_HOME` 回退到仓库下 `.hf_cache`。
- `rag_pipeline_parse_graph_chat`：在 import LightRAG 前设置离线；交互循环区分「空行」与「退出词」；处理 `EOFError` / `KeyboardInterrupt`；文档字符串补充 `--query-only` 示例。
- 文档：新增/充实 `docs/文档解析灌库与图谱问答脚本说明.md`，`README_zh` 补充链接。

**规模（提交当时）：** 6 个文件，约 +432 / −89 行。

### 代码改动

**1）uv：CUDA 13.0 轮子索引与版本覆盖**

**改动目的（当时）：** PyPI 默认解析到的 Windows **小体积 CPU 轮子**与期望的 **GPU 环境**不一致，导致「以为在用 CUDA 实际在 CPU」或反复被覆盖安装。通过 **显式 cu130 索引 + override**，让 `uv sync` / `uv run` 在目标环境上**稳定落在带 CUDA 后缀的 torch/torchvision**。

```toml
# pyproject.toml
[[tool.uv.index]]
name = "pytorch-cu130"
url = "https://download.pytorch.org/whl/cu130"
explicit = true

[tool.uv.sources]
torch = { index = "pytorch-cu130" }
torchvision = { index = "pytorch-cu130" }

[tool.uv]
override-dependencies = [
    "torch==2.11.0+cu130",
    "torchvision==0.26.0+cu130",
]
```

> 其他平台或 CUDA 版本若解析失败，需按本机环境修改或暂时移除上述段。

**2）本地 HF 嵌入：Hub 快照路径与设备选择**

**改动目的（当时）：** ① **设备**：避免手写死 `cpu`，在有 NVIDIA / Apple GPU 时自动用上加速。② **离线**：模型已下载后，通过环境变量在 import 前切断 Hub，减少超时与无网失败。③ **快照路径**：从 `HF_HOME` 下 Hub 目录解析 `snapshots`，尽量**只读本地**。④ **`HF_HOME` 回退**：团队把缓存放在仓库 `.hf_cache` 时，未设环境变量也能自动找到。

```python
# raganything/local_hf_embedding.py
def _hub_snapshot_dir(repo_id: str, hf_home: str) -> Path | None:
    """Resolve HF hub cache snapshot path for ``org/name`` (no network)."""
    if "/" not in repo_id.strip():
        return None
    org, name = repo_id.strip().split("/", 1)
    if not org or not name:
        return None
    repo_dir = Path(hf_home) / "hub" / f"models--{org}--{name}"
    ref = repo_dir / "refs" / "main"
    if not ref.is_file():
        return None
    rev = ref.read_text(encoding="utf-8").strip()
    if not rev:
        return None
    snap = repo_dir / "snapshots" / rev
    if snap.is_dir() and (snap / "modules.json").is_file():
        return snap
    return None


def ensure_hf_home_from_repo_fallback(repo_root: str | os.PathLike[str] | None) -> None:
    """If HF_HOME is unset, use <repo_root>/.hf_cache when that directory exists."""
    if os.getenv("HF_HOME"):
        return
    base = repo_root
    if base is None:
        return
    candidate = os.path.join(str(base), ".hf_cache")
    if os.path.isdir(candidate):
        os.environ["HF_HOME"] = candidate


def _resolve_embedding_device() -> str:
    explicit = (os.getenv("HF_EMBED_DEVICE") or os.getenv("EMBEDDING_DEVICE") or "").strip()
    if explicit:
        return explicit.lower()
    try:
        import torch
        if torch.cuda.is_available():
            return "cuda"
        if hasattr(torch.backends, "mps") and torch.backends.mps.is_available():
            return "mps"
    except ImportError:
        pass
    return "cpu"
```

```python
# raganything/local_hf_embedding.py（make_local_hf_embedding_func 开头）
offline = (os.getenv("HF_EMBED_OFFLINE") or "").strip().lower() in (
    "1",
    "true",
    "yes",
)
if offline:
    os.environ["HF_HUB_OFFLINE"] = "1"
    os.environ.setdefault("TRANSFORMERS_OFFLINE", "1")
```

**3）交互式问答：退出词集合、空行不退出、Ctrl+C / EOF**

**改动目的（当时）：** 原先**空行即退出**，和长问题多行粘贴、或习惯性敲空行的终端用法冲突；同时需要明确的 **退出口令** 与 **Ctrl+C / EOF** 行为，避免误退或卡死无响应。`--query-only` 等示例写进 docstring，方便复制「只问答、不复灌库」的命令。

```python
# scripts/rag_pipeline_parse_graph_chat.py
_QUIT_TOKENS = frozenset(
    {
        "exit",
        "quit",
        "bye",
        "/exit",
        "/quit",
        ":q",
        "!q",
        "退出",
        "再见",
    }
)


def _interactive_should_quit(line: str) -> bool:
    t = (line or "").strip()
    if not t:
        return False
    if t in _QUIT_TOKENS:
        return True
    return t.lower() in _QUIT_TOKENS
```

```python
# scripts/rag_pipeline_parse_graph_chat.py（循环片段）
while True:
    try:
        q = await asyncio.to_thread(input, "Q> ")
    except (EOFError, KeyboardInterrupt):
        print("\n[exit]", flush=True)
        break
    q = (q or "").strip()
    if _interactive_should_quit(q):
        break
    if not q:
        continue
    try:
        ans = await rag.aquery(q, mode=query_mode, vlm_enhanced=False)
        print(ans or "", flush=True)
    except (asyncio.CancelledError, KeyboardInterrupt):
        print("\n[exit]", flush=True)
        break
```

说明：`fa0ed92` 中 `aquery` 仅有 `mode` 与 `vlm_enhanced`；后续提交 `a2995b3` 起在同一位置增加 `**_query_extras_from_env()`。若需与当前工作区完全一致，以 `scripts/rag_pipeline_parse_graph_chat.py` 为准，并用 `git show fa0ed92` 核对当时 diff。

查看完整差异：

```bash
git show fa0ed92
```

---

*以上条目按 Git 提交时间倒序排列。新增一节时必须包含：**版本标题**、**本版本目的（当时）**、各条 **改动目的（当时）** + **代码块**（见 `.cursor/rules/changelog-with-codeblocks.mdc`）。以 `git show <hash>` 为准核对细节。*
