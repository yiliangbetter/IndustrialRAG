flowchart TD
    U[用户输入] --> G[计算 score<br/>aquery_data 轻量检索]
    G -->|score >= upper| R[现有 aquery 全流程]
    G -->|lower <= score < upper| B[Case B: LLM+k 改写+再检索]
    G -->|score < lower| A[Case A: LLM+k 改写+再检索]
    B --> C{候选问法}
    A --> C
    C -->|用户选择 / 自动首条| R
    C -->|仍无合格候选| H[提示用户换说法<br/>不调用 RAG]
    R --> SSE[回答 + 配图]
