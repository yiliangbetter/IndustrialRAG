# PR #47（PR-A）下一步工作摘要

> **给后续会话 / Agent**：先读本文 + [`docs/PR拆分合入主线操作说明.md`](PR拆分合入主线操作说明.md)。  
> 大改（`utils.py` / `image_query_refs.py` 去 hardcode、拆文件）**另开会话**做；本摘要为交接材料。  
> 快照时间：2026-07-25；数据来源：GitHub PR #47 review threads（当时 **24 unresolved / 60 total**，`mergeStateStatus=BLOCKED`）。

---

## 0. 分支与拆分边界（勿越界）

| PR | 分支 | 本摘要相关 |
|----|------|------------|
| **PR-A** #47 | `pr-a-core-engine` | 核心引擎瘦身 + 小修；**不要**塞 `pack_client*` / `client_*` / `web/` |
| **PR-B** | `pr-b-web-ui` | 客户端打包、Web、澄清接线、`CLIENT_TUNING_KEYS` |
| **PR-C** | `pr-c-docs` | 文档 |
| **PR-D** | `pr-d-dev-tooling` | bench/eval 工具 |

权威拆分说明：`docs/PR拆分合入主线操作说明.md`（须长期保留，勿当误暂存删掉）。

---

## 1. 当前 #47 状态

- **未解决线程：24**（多数是 yiliangbetter 提问 + 你的回复，尚未 resolve）
- Merge：**BLOCKED**（ruleset 需 2 Approves；且评审意见未清）
- 本地已做、未全部进 PR 的：`config/env.example` + 根 `env.example` 合并（见下）；**不要**把打包脚本放进 #47

---

## 2. 按评审结论分类的待办

### A. 可在 #47 内立刻做（瘦身 / 小改）— 本会话或短会话

| 项 | 评审结论 | 动作 |
|----|----------|------|
| `config/query_steering_profiles.json` | 不该进 main；你已回「拿掉/改示例」 | **从 PR-A 移除**或改 `config/examples/` 并注明非运行时必需（代码实际用 `MACHINE_PROFILES`，JSON 未加载） |
| `raganything/naive_relevance.py` | 弃用则全删 | **删除模块**，并清 `query.py` / hooks / Web 侧 import（Web 在 B，A 上先清本 PR 能碰到的引用） |
| `raganything/query.py::score_naive_relevance` | 不需要 | **删除** method |
| `scripts/naive_relevance.py` | 可删 | **删除** shim |
| `scripts/strip_table_flat_from_chunks.py` | 迁移脚本，可不进 main | **移出 PR-A** |
| `scripts/build_table_matrix_from_storage.py` | 对方回头自看；属 tooling | **建议移出 PR-A → PR-D**（或随 table 决策） |
| `tests/testparser_ingest_coalesce.py` 硬编码路径 | 换环境会挂；你回可不合入 | **改相对/tmp 路径**或移出；优先修好再留 |
| `pipeline_rerank` 多套 RELEASE | 对方问：是否只留 `after query` | **收敛触发点**：默认只保留 `RERANK_RELEASE_AFTER_QUERY`（或文档说明唯一推荐）；删/弃用未使用的 after_predict/after_gate 若产品未用 |
| `env.example` 双份 | 对方指出两个；你回后期合并 | **在 A 完成合并**：权威=`config/env.example`（A/B/C/D 分层），根=`短指针`（本地已改，需 commit + push；**勿带 pack 脚本**） |
| `parser.py` 取消灌库 | 对方：**明白，可以加** | **保留**；线程可 resolve |
| `table_matrix` | 对方：**先保留，回头研究** | **#47 保留**；补一句 design 注释即可，大讨论不阻塞 |
| `rag_pipeline_parse_graph_chat.py` | 已解释用途 | 可 resolve；可选加强模块 docstring |

做完后：在对应 thread **回复 + resolve**；再催第 2 个 Approve。

### B. 明确留给「合入后 / 新 PR」— **另开会话做大改**

| 项 | 评审结论 | 动作 |
|----|----------|------|
| `raganything/utils.py` 过大 + hardcode | 拆模块；hardcode 要改；**可再提新 PR**；合入后再细 review | **Follow-up refactor PR**：按功能拆（image align / segment coalesce / ingest segments…），先 re-export，再去领域 hardcode |
| `scripts/image_query_refs.py` | hardcode 全去掉；文件过大（勿超 ~1000 行）；**配图也不能绑「保养」等业务词**（换文档会 fail）；页码/图注类结构字段可留 | **独立大改会话**：去 `_KNOWN_MACHINE_NAMES`、话术字段、Q 题正则等；只留 parser 字段 + term overlap + env 阈值；拆文件 |

### C. 已口头对齐、主要差 resolve

- **parser 取消逻辑**：可加 → resolve  
- **table_matrix 先留**：对方回头看 → 可 resolve（或留一条「design follow-up」）  
- **utils 拆分时机**：#47 后 → resolve，并链到 follow-up issue/PR  

---

## 3. 建议执行顺序

```text
1) PR-A 瘦身 commit（naive 全删、steering JSON、strip/backfill 移出、rerank release 收敛、测试路径、env 合并）
2) 回复并 resolve 已对齐线程（parser / table 先留 / utils 拆分延后）
3) push pr-a-core-engine，等第 2 Approve，合 #47
4) 新会话：utils + image_query_refs 大改（另开 PR，勿塞回已合的 A）
5) A 合入后：开 PR-B（Web + 客户端打包）；CLIENT_TUNING_KEYS 扩集合放 B
```

---

## 4. 大改会话开场提示（可直接粘贴）

```text
请先读：
1. docs/PR拆分合入主线操作说明.md
2. docs/PR47_下一步工作摘要.md
3. .cursor/rules/pr-split-merge-guide.mdc
4. .cursor/rules/rag-image-matching.mdc

当前任务（#47 合入后的 follow-up，或并行准备分支）：
- 拆分 raganything/utils.py（先 re-export，不改行为，再去 hardcode）
- 重审 scripts/image_query_refs.py：去掉领域/话术 hardcode，控制单文件体量，只保留结构字段+term overlap+env

不要把 PR-B 的 pack/web/client 脚本放进 PR-A。
工作分支约定：大改用新 branch（例如 pr-a2-utils-image-refactor），base 用最新 main 或合入后的 main。
```

---

## 5. 本地未推送提醒（截至写摘要时）

以下可能仍仅在本地（以 `git status` / `git log` 为准）：

- `docs/PR拆分合入主线操作说明.md` + `.cursor/rules/pr-split-merge-guide.mdc`（多分支 cherry-pick）
- `config/env.example` / 根 `env.example` 合并（未 commit 或未 push）

推送前确认 **#47 不含** `scripts/pack_client*`、`client_*`。
