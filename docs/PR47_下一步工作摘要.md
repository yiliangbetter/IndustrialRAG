# PR #47（PR-A）下一步工作摘要

> **给后续会话 / Agent**：先读本文 + [`docs/PR拆分合入主线操作说明.md`](PR拆分合入主线操作说明.md)。  
> 大改（`utils.py` / `image_query_refs.py` 去 hardcode、拆文件）**另开会话**做；本摘要为交接材料。  
> 快照时间：2026-07-25；数据来源：GitHub PR #47 review threads（当时 **24 unresolved / 60 total**，`mergeStateStatus=BLOCKED`）。

---

## 0. 分支与拆分边界（勿越界）

| PR | 分支 | 本摘要相关 |
|----|------|------------|
| **PR-A** #47 | `pr-a-core-engine` | 核心引擎瘦身 + 小修；**不要**塞 `pack_client*` / `client_*` / `web/` |
| **PR-B** | `pr-b-web-ui` | 客户端打包、Web、澄清接线、`CLIENT_TUNING_KEYS` |
| **PR-C** | `pr-c-docs` | 文档 |
| **PR-D** | `pr-d-dev-tooling` | bench/eval 工具 |

权威拆分说明：`docs/PR拆分合入主线操作说明.md`（须长期保留，勿当误暂存删掉）。  
**瘦身 ↔ 全量大改同步**：见该文档 **「十一、PR-A 瘦身 ↔ lhq-rag-dev 全量大改同步」**（先 A 瘦身 → merge 进 lhq → 大改并测 → 拣回 A → 再合 main）。

---

## 1. 当前 #47 状态

- **未解决线程：24**（多数是 yiliangbetter 提问 + 你的回复，尚未 resolve）
- Merge：**BLOCKED**（ruleset 需 2 Approves；且评审意见未清）
- 本地已做、待 commit/push：`config/env.example` + 根 `env.example` 合并（见下）；**不要**把打包脚本放进 #47

---

## 2. 按评审结论分类的待办

### A. 可在 #47 内立刻做（瘦身 / 小改）— 本会话或短会话

| 项 | 评审结论 | 动作 |
|----|----------|------|
| `config/query_steering_profiles.json` | 不该进 main；你已回「拿掉/改示例」 | **已删除**（运行时用 `MACHINE_PROFILES`；env 已去掉 `RAG_QUERY_STEERING_PROFILES`） |
| `raganything/naive_relevance.py` | 弃用则全删 | **已删除**（连同 exports / env 注释） |
| `raganything/query.py::score_naive_relevance` | 不需要 | **已删除** |
| `scripts/naive_relevance.py` | 可删 | **已删除** |
| `scripts/strip_table_flat_from_chunks.py` | 迁移脚本，可不进 main | **已删除**（不并入其他 PR；库内 `strip_table_flat_from_content` 仍保留） |
| `scripts/build_table_matrix_from_storage.py` | 离线补建 tooling；不需要 | **已删除**（不并入其他 PR；`table_matrix` 库能力仍保留，新灌库走 ingest 钩子） |
| `tests/testparser_ingest_coalesce.py` | 本机路径硬编码；仅为临时测试 | **已删除**（不进 main / 不并入其他 PR） |
| `env.example` 双份 | 对方指出两个；你回后期合并 | **已完成（本地未 commit）**：权威=`config/env.example`（A/B/C/D），根=`短指针`；瘦身时已去掉 naive / steering JSON 键 |
| `parser.py` 取消灌库 | 对方：**明白，可以加** | **保留**；线程可 resolve |
| `table_matrix` | 对方：**先保留，回头研究** | **#47 保留**；补一句 design 注释即可，大讨论不阻塞 |
| `rag_pipeline_parse_graph_chat.py` | 已解释用途 | 可 resolve；可选加强模块 docstring |

> **不在 #47 短会话做（已约定另开会话）**：`pipeline_rerank` RELEASE 收敛、一切 hardcode 清理（含 `utils.py` / `image_query_refs.py`）。

做完后：在对应 thread **回复 + resolve**；再催第 2 个 Approve。

### B. 明确留给「另开会话」— 大改 / 调优（勿塞回 #47 急合）

| 项 | 评审结论 | 动作 |
|----|----------|------|
| `pipeline_rerank` 多套 `RERANK_RELEASE_*` | 对方问是否只留一种（如 after query） | **另开会话**：审计实际调用点，收敛为单一推荐策略并删未用路径 |
| `raganything/utils.py` 过大 + hardcode | 拆模块；hardcode 要改；**可再提新 PR** | **另开会话 / follow-up PR**：按功能拆（先 re-export），再去领域 hardcode |
| `scripts/image_query_refs.py` | hardcode 全去掉；控体量；**配图不能绑「保养」等业务词** | **另开会话**：去机型名单/话术/Q 题正则；只留结构字段 + term overlap + env |

### C. 已口头对齐、主要差 resolve

- **parser 取消逻辑**：可加 → resolve  
- **table_matrix 先留**：对方回头看 → 可 resolve（或留一条「design follow-up」）  
- **utils 拆分时机**：#47 后 → resolve，并链到 follow-up issue/PR  

---

## 3. 建议执行顺序（与「十一」同步流程一致）

```text
1) 在 pr-a-core-engine 瘦身 → commit → push（更新 #47；可暂不合 main）
   —— 不含 rerank RELEASE、不含 hardcode 大改
2) git checkout lhq-rag-dev && git merge pr-a-core-engine
   —— 冲突：保留 Web/client；接受 A 的删除/瘦身 → push lhq
3) 回复并 resolve 已对齐线程（parser / table 先留；utils·配图·rerank 标另会话）
4) 另开会话：在 lhq-rag-dev 做大改 + 全量测
5) cherry-pick / 拣文件回 pr-a-core-engine（禁止 merge lhq→pr-a）→ push #47
6) 再合 #47 进 main；之后开 PR-B（Web + 客户端打包）
```

详见：`docs/PR拆分合入主线操作说明.md` §十一。

---

## 4. 大改会话开场提示（可直接粘贴）

```text
请先读：
1. docs/PR拆分合入主线操作说明.md（尤其 §十一：瘦身↔lhq 同步）
2. docs/PR47_下一步工作摘要.md
3. .cursor/rules/pr-split-merge-guide.mdc
4. .cursor/rules/rag-image-matching.mdc

开工前确认：lhq-rag-dev 已 merge 过最新 pr-a-core-engine（含瘦身）。
若未 merge：先完成 §十一第 2 步，再大改。

本会话任务：
1) pipeline_rerank：收敛 RERANK_RELEASE_*（评估是否只保留 after query）
2) 拆分 raganything/utils.py（先 re-export，再去 hardcode）
3) 重审 scripts/image_query_refs.py：去掉领域/话术 hardcode，控体量

全量测在 lhq-rag-dev；测绿后 cherry-pick/拣文件回 pr-a（禁止 merge lhq→pr-a）。
不要把 PR-B 的 pack/web/client 塞进 PR-A。
```
---

## 5. 本地未推送提醒（截至写摘要时）

以下可能仍仅在本地（以 `git status` / `git log` 为准）：

- `docs/PR拆分合入主线操作说明.md` + `.cursor/rules/pr-split-merge-guide.mdc`（多分支 cherry-pick）
- `config/env.example` / 根 `env.example` 合并（本地已改，待 commit + push）
- 本文与 §十一 同步说明的后续 commit

推送前确认 **#47 不含** `scripts/pack_client*`、`client_*`。
